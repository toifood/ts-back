SHOULD ASSET LOG
prompt: review and update ARCHITECTURE ASSET decisions for 2026Q3
path: should/ARCHITECTURE-ASSET-2026Q3.md
target: {repo}

INSTRUCTION FOR AI MODEL:

YOU MAY READ AND UPDATE EXISTING ENTRIES AS THE SYSTEM EVOLVES.
ADD NEW ENTRIES AT THE TOP FOR NEW TOPICS; UPDATE IN PLACE FOR EXISTING ONES.

FORMAT: ## ASSET:{NAME} {YYYY-MM-DD HH:MM} → {CONTENT}

####### <!-- ANCHOR MARKER - ADD OR UPDATE ENTRIES DIRECTLY BELOW THIS LINE -->
## ASSET:ARCHITECTURE 2026-09-07 08:18 ▸ Deployment topology, route versioning, and schema-hardening conventions now in place

**Deployment topology (per README.md):** single Mac mini M4 running two macOS accounts — `jayreck` hosts this Node.js API (`:3000`) and the colocated PostgreSQL instance (`:5432`); `jayagent` hosts Ollama (`:11434`, `qwen2.5:7b`) reached only via `127.0.0.1`. Public ingress is a Cloudflare Tunnel to `toifood.co.nz`; `src/index.ts` sets `app.set("trust proxy", 1)` to respect the tunnel's `X-Forwarded-For`.

**Route versioning convention introduced.** `src/index.ts` mounts the full route set twice: a new `/1-1-6/{auth, api/*, system/*}` namespace and the original unprefixed paths, both pointing at the same `src/routes/*.ts` modules, so old mobile builds keep working while new builds migrate to the versioned prefix.

**Domain/module architecture is consistent across the codebase.** `src/domains/*` (recipe, user, follow, review, report, bookmark, list, note, draft, insight, pantry, ingredient, emailpin, agent) own Prisma-backed entities and business logic; `src/modules/*` (rate, language, role, generate, cookie, email, push, seo, ogimg, analysis, preference, idp, appstore/playstore, highlight, color, content) own cross-cutting concerns. Each module exposes exactly one `register.ts` as its public door — peer modules import through it rather than reaching into `constants.ts`/`utils.ts` directly (explicitly called out in `src/modules/rate/register.ts`'s header comment).

**Schema hardening: enum-vs-string split is now a documented, deliberate rule.** The `20260812160000_enum_to_string_categories` migration converted 4 volatile/cosmetic fields (`ListColor`, `FlagType`, `ReportReason`, `ReviewCategory`) from Postgres enums to validated `TEXT`, while keeping small, stable, logic-driving fields (`UserRole`, `Visibility`, `PreferenceStyle`, `RecipeProvider`, `ReportStatus`) as real DB enums — `prisma/schema.prisma`'s `PreferenceStyle`/`RecipeProvider` comments spell out the criterion (stable + load-bearing → enum; volatile/evolving → app-validated string) for future schema decisions.

**Moderation/lifecycle timestamp convention standardized.** `Follow`, `Review`, `Note`, `Draft`, `SavedListField`, and `UserInsight` all use the same shape: an always-set `requestedAt` plus parallel outcome timestamps (`removedAt`/`acceptedAt`/`rejectedAt`) instead of a single `status` + `updatedAt` pair — giving an auditable per-row transition history without a separate history table, per the inline comments in `prisma/schema.prisma`.
## ASSET:ARCHITECTURE 2026-08-31 08:49 ▸ New `difficulty` preference axis; `Property` decoupled onto its own `PropertyType` enum (adds ingredient/mealtime); `Preference` gains a matching index; schema confirmed at 20 models

Delta since the 2026-08-24 entry — thin-controller domains/modules split, role/rate topology, Agent retirement, and infra/ops below this entry are otherwise unchanged.

**New `difficulty` preference axis, same convention as `method` (`20260826000000_add_difficulty_preference_and_recipe_column`).** `PreferenceType` gains a 6th value (`difficulty`); `Recipe.difficulty` is added as a per-generation snapshot column (`easy`/`intermediate`/`advanced`, single-select) — same "recorded, not derived" reasoning already established for `Recipe.method`, since a recipe's snapshot can outlive changes to the caller's current standing preference. `modules/preference/constants.ts`'s `CATEGORY_PREFERENCE_DIFFICULTY` is the new source of truth. Already reflected in `contracts.ts` (`difficulty?: string | null` / `Difficulty` type) — no contract drift here, unlike the multi-quarter `shared/` drift this log tracked before that package was removed.

**`Property` decoupled onto its own `PropertyType` enum, no longer reusing `PreferenceType` (`20260828000000_property_type_own_enum`).** `PropertyType` adds two Property-only axes — `ingredient` and `mealtime` — that must never become valid `Preference` values (per `schema.prisma`'s comment: `mealtime` specifically is set at generate/save time, not user-chosen, and `ingredient` has no standing-preference counterpart yet). `Preference` itself and its enum are untouched by this migration. Migration comment notes it was hand-written via `prisma migrate deploy` rather than `prisma migrate dev` because the shadow-database replay is broken — see ISSUE log for the underlying `UserInsight` gap this surfaces.

**`Preference` gains `@@index([type, value])`, mirroring `Property`'s existing index (`20260828030000`).** Both now support the same `matchUsers`/`matchLists`-style self-join filter (`WHERE type=X AND value=Y AND ownerId != mine`) at the database level rather than one of the two tables doing an unindexed scan.

**Same-day round-trip: `Personality` table added then reverted, `User.ageRange`/`gender` stay put (`20260828010000` → `20260828020000`).** A `Personality` table (mirroring `Property`'s one-row-per-`(userId, type)` shape, for a future `matchUsers` cross-user self-join on demographics) was created and backfilled from `User.ageRange`/`gender`, then reverted the same day back onto plain `User` columns with data copied back losslessly — decided in favor of building future user-matching on `Preference`'s existing axes instead of a bespoke demographics table. Net effect on live schema: zero.

**Schema state: 20 models, confirmed via direct count of `schema.prisma`** (`User`, `Follow`, `Review`, `ReviewCategoryRating`, `Recipe`, `RecipeReport`, `UserReport`, `Bookmark`, `RecipeReview`, `SavedList`, `SavedListField`, `SavedListItem`, `Property`, `Note`, `Draft`, `Notification`, `Preference`, `PantryItem`, `UserInsight`, `EmailPin`). No net model change since 2026-08-24 — this quarter's churn was enum/column/index-level only (the `Personality` add+revert nets to zero); noting the recount here since the running model-count figure hadn't been independently re-verified by grep in a few quarters.
## ASSET:ARCHITECTURE 2026-08-24 06:35 ▸ Agent table retired onto User; four cosmetic enums converted to app-validated TEXT; RecipeStyle→PreferenceStyle; UserInsight reshaped into a 6-category slot system; schema at 19 models

Delta since the 2026-08-10 entry — thin-controller domains/modules split, role/rate topology, and infra/ops below this entry are otherwise unchanged.

**`Agent` table retired; language verdict moved directly onto `User` (`20260810000000_retire_agent_table`).** The 1:1 `Agent` table (added 2026-07-23, previously the single owner of the rolling `language`/`languageUpdatedAt` verdict) is dropped after copying its two columns onto `User` directly. `src/domains/agent/register.ts`'s `updateUserLanguage()` now does a plain `prisma.user.update({ data: { language, languageUpdatedAt } })` — confirmed no leftover `prisma.agent` references anywhere in `src/`. One fewer join for every moderation write, at the cost of one fewer dedicated table if a second per-user AI-agent concept shows up later.

**Four cosmetic/volatile enums converted from Postgres enums to app-validated `TEXT` (`20260812160000_enum_to_string_categories`).** `ListColor`, `FlagType`, `ReportReason`, `ReviewCategoryRating.category` all move to plain strings, validated against their existing `constants.ts` tables instead of a DB-level `CREATE TYPE`. The migration's own comment states the reasoning: safe/lossless (`::TEXT` cast preserves every row), and reserved for values that are "volatile/cosmetic, not stable+load-bearing" — `schema.prisma`'s comment on the new `PreferenceStyle` enum (below) explicitly contrasts itself against this batch as the case that *should* stay a real enum. This directly resolves the 2026-07-20 entry's "Minor" flag about `ListColor` being hand-duplicated as a local array — that duplication is now moot since the source of truth is app-side constants either way.

**`Recipe.recipeStyle` renamed to `Recipe.style`; its backing enum created then immediately renamed (`20260817000000` → `20260820000000` → `20260820010000`).** Three-step, same-week churn: create `RecipeStyle` enum and cast the column to it (verified 100% clean classic/creative/null beforehand, no CASE mapping needed) → rename column `recipeStyle`→`style` → rename the enum itself `RecipeStyle`→`PreferenceStyle`. End state per the schema comment: `style` is a generation-time snapshot of what was, conceptually, first a `Preference{type:'style'}` user setting — the rename makes the column name stop implying it's the enum's only reason to exist.

**`RecipeProvider` enum added, replacing a free-text `Recipe.provider` (`20260818010000`).** Cast verified against live data first (323 `claude`, 107 `ollama`, 2 `null`, no stray values) — same enum-if-stable/TEXT-if-volatile split as the batch above; `provider` is small, stable, and load-bearing for prompt branching, so it stays a real DB enum unlike the four demoted this quarter.

**`UserInsight` reshaped from a plain history row into a slot-based system with 6 categories (`20260817010000` → `20260819000000`).** Deduped to one row per `(userId, category)` (kept newest by `createdAt`, then dropped `createdAt` in favor of `requestedAt`), gained a `pending/accepted/rejected` status with `requestedAt`/`acceptedAt`/`rejectedAt`/`removedAt` — same boundary-timestamp convention as `Follow`/`Review`/`Note`, except here "accepted" means the AI analysis itself completed and "rejected" means it fell back to template text, not a moderation outcome. `category` (`dietary|cuisine|style|pantry|mealType`) gained a 6th value, `method`, the day after `Recipe.method` and the `method` `PreferenceType` axis shipped. `suggestion`→`body` and `context`→`title` were renamed for shape parity with `Note`; `context` itself had only existed for two days (`20260817030000` added it as a drift-catch fix, `20260819000000` renamed it three migrations later).

**Two new models with no precedent yet: `Draft` (profile-edit moderation, live) and `Notification` (push-delivery tracking, unused).** `Draft` holds one pending/accepted/rejected candidate value per `(userId, type)` for `name`/`username`/`bio` — same boundary-timestamp + `languageVerdict` shape as `Note`, but distinguished by promoting into the matching `User` field only on acceptance, so a rejected field keeps its last-approved live value rather than being cleared. `Notification` mirrors `Note`'s id/title/body/status shape for push delivery (`sentAt`/`failedAt`/`readAt`) but deliberately has no `languageVerdict` (backend-authored copy, nothing to moderate) and no device-token storage yet — see ISSUE log for the migration gap affecting both.

**`shared/` package removed from this repo entirely.** No `shared/` directory remains in the tree (confirmed via full recursive tree listing) — the cross-repo shared-types drift tracked across five straight quarterly entries (2026-07-06 through 2026-08-03) is now moot by removal rather than by resolution; not re-litigating unless it reappears elsewhere.

**Schema state.** 19 models, net +1 since 2026-08-10 (18): −`Agent`, +`Draft`, +`Notification`.
## ASSET:ARCHITECTURE 2026-08-10 06:48 ▸ Boundary-timestamp lifecycle shape now covers Note/SavedList/EmailPin too; preferences unified into one table with country + method axes; schema at 18 models

Delta since the 2026-08-03 entry — thin-controller domains/modules split, Agent-based language moderation, role/rate topology, and infra/ops below this entry are otherwise unchanged.

**The Follow/Review boundary-timestamp lifecycle shape (`status` + `requestedAt`/`acceptedAt`/`rejectedAt`/`removedAt` [+ `languageVerdict`/`languageUpdatedAt` where moderated]) now extends across almost the whole schema.** `20260803010000_note_list_lifecycle` → `20260804060000_savedlist_split_title_description` land it on `SavedList`/`SavedListItem`/the new `Note` table; `20260806010000_emailpin_lifecycle` lands it on `EmailPin` (`type`/`status`/`requestedAt`/`acceptedAt`/`rejectedAt` alongside the existing `attemptCount`/`pinHash`). `SavedList` itself briefly carried these fields directly (`20260803010000`) before `20260804060000_savedlist_split_title_description` moved them into a new `SavedListField` table (`type: title|description`), mirroring `Note`'s `recipeId`+`type` pattern — title and description are now moderated and gated independently (`src/domains/list/register.ts runListCombinedModeration` writes one combined-text verdict to both field rows but each has its own accept/reject state, so a rejected description no longer takes the list's title down with it).

**Note system unified: per-recipe notes and standalone notes are now one table.** `RecipeNote` (created `20260803010000`) was retired one day later (`20260804010000_retire_recipenote_for_note`) in favor of `Note.recipeId`/`type` — recipe-scoped notes (description/ingredient/method, written via `upsertRecipeNote` in `src/domains/recipe/register.ts`) and freeform standalone notes (`src/domains/note/register.ts` `createNote`/`updateNote`) now share one table, one moderation resolver (`runNoteModeration`, `LANGUAGE_NOTE` preset — previously flagged as shipped-unwired, now actually wired end to end), and one `getMyNotes` listing that surfaces both kinds together.

**User preferences unified into a single `Preference` table with 5 axes (`dietary`/`country`/`style`/`servings`/`method`), replacing 3 different storage shapes.** `20260805000000_unify_user_preferences` renames `DietaryPreference` → `Preference`, adds a `PreferenceType` enum, and retires the separate `User.recipeStyle` (scalar column) and `User.continentPreferences` (array column) that `PATCH /users/me/preferences` used to write independently. `modules/preference/register.ts getUserPreferences()`/`replacePreferences()` is now the single read/write path for all 5 axes; `modules/preference/constants.ts` is the new single owner of option catalogs (dietary labels, the country list, servings, methods, section copy), replacing the old scattered `generate/constants.ts` copy of `COUNTRY_REGIONS`.

**Continent taxonomy replaced by country-level granularity (`20260808000000_continent_to_country`).** The old 7-broad-region `continent` `PreferenceType` value and `Recipe.continent` column are renamed to `country`, backed by a new 65-demonym `COUNTRY_FLAGS` table with flag emoji folded into each label (`modules/preference/constants.ts`). A new `method` preference axis (`20260809000000_add_preference_method`: Bake/Grill/Fry/Steam/Boil/Roast/Raw/Slow Cook/Stir-fry/Air Fry, multi-select, Claude-provider-only) was added the following day — `PreferenceType` now has 5 values.

**Review's naming aligned with Follow's (`20260803000000`/`20260804000000`).** `Review.userId` → `reviewerId`, and a new `revieweeId` (denormalized copy of `Recipe.userId` at submit time, backfilled from the recipe) lets "reviews about my recipes" queries filter directly without a join — same reasoning and same column name convention as `Follow.followeeId`.

**Dead password-reset flow removed (`20260806000000_drop_password_reset_token`).** `PasswordResetToken` and its link-based reset flow are confirmed dead (the app's "Forgot Password" screen uses `requestPin`/PIN-based sign-in exclusively) and dropped.

**Schema state.** 18 models, up from 17 on 2026-08-03: net +1, composed of +`Note`, +`SavedListField`, −`PasswordResetToken` (`RecipeNote` was created and dropped within the same window, net zero).
## ASSET:ARCHITECTURE 2026-08-03 07:18 ▸ Role/rate split into modules/role + modules/rate replacing src/lib/premium.ts + src/middleware/rateLimit.ts; UserRole expands to 4 tiers (free/pro/super_pro/admin); Review.stars dropped, EmailPin gains pendingEmail

Delta since the 2026-07-27 entry — thin-controller domains/modules split, Agent-based language moderation, and schema topology below this entry are otherwise unchanged.

**Role/rate logic relocated into `modules/role` + `modules/rate` (migration `20260728000000_user_role_super_pro`).** The previously separate `src/lib/premium.ts` and `src/middleware/rateLimit.ts` no longer exist in the tree; their responsibilities split into two peer modules following the same "one public door" convention as `modules/language`: `modules/role` owns identity (`getTier()` re-derives role from the DB every request, never trusts the JWT; `requireAdmin` re-reads per request too) and `modules/rate` owns policy (`RATE_PRESETS` per tier, Redis-backed atomic Lua INCR+EXPIRE via `modules/rate/utils.ts`, unchanged fail-open-with-warning behavior on Redis failure). `rate/constants.ts` imports `Tier` directly from `role/constants.ts` (documented exception to the one-public-door rule: plain types flow constants-to-constants between peer modules; behavior-gating functions like `getTier`/`requireAdmin` still route through `register.ts`).

**`UserRole` expands from 3 tiers to 4 (`free/pro/super_pro/admin`), replacing `premium`.** The migration recreates the Postgres enum (Postgres has no `ALTER TYPE ... DROP VALUE`) after confirming 0 rows used `premium`. Role is now stated as the sole source of truth for tiering — `premiumSince`/`premiumUntil` timestamps remain on `User` but are display-only, no longer read for access decisions (per the migration's own comment and `modules/role/constants.ts`). New limits: free 3 ollama/2 claude, pro 10/5, super_pro 30/15, admin unlimited (`modules/rate/constants.ts`) — pro/super_pro replace the old single "premium" tier's 10/5 cap. A `requireTier(minTier)` gate (rank-ordered via `TIER_RANK`) was added alongside but isn't mounted on any route yet (see ISSUE log).

**Review simplified to category-only rating (migration `20260802000000_review_drop_stars`).** The top-level `Review.stars` column is dropped; `ReviewCategoryRating` (taste/difficulty/authentic) is now the only rating surface, and `src/domains/review/register.ts`'s recipe-level average is computed by flattening `categoryRatings` across all reviews rather than reading a single `stars` field. `RecipeReview` (the older, separate simple-star model) is unaffected and still coexists.

**`EmailPin.pendingEmail` added (migration `20260728050000`).** Confirms and extends the existing dual-purpose PIN design already noted in the schema (`pendingEmail: null` verifies the account's current email at signup/login; set = verifying a new email) — the column existed in `prisma/schema.prisma` comments before this migration made it real.

**Schema state.** 17 models, unchanged count from 2026-07-27 (this quarter's changes are column/enum-level: `UserRole` tier expansion, `Review.stars` drop, `EmailPin.pendingEmail` add, plus the `Follow`/`Review` opened-at churn — see ISSUE log for the resulting drift).
## ASSET:ARCHITECTURE 2026-07-27 07:18 ▸ Thin-controller domains/modules split now fully wired; Agent-based language moderation (Review/Follow comments) shipped; CookRecord removed; schema at 17 models

Delta since the 2026-07-20 entry — topology, AI-provider selection, route versioning, Redis rate limits, flagging/moderation admin queue, username system, and list color theming are unchanged below this entry.

**Thin-controller domains/modules architecture.** `src/routes/*.ts` now only parse the request, call one function from `src/domains/{domain}/register.ts` or `src/modules/{module}/register.ts`, and translate the returned `ApiResult` (`{status, body}`) straight into the HTTP response — confirmed against `src/routes/reviews.ts`/`agents.ts` and their `domains/review`/`domains/agent` counterparts, no Prisma calls or business logic left in route files. Domains own DB access and app rules (`agent`, `auth`, `bookmark`, `follow`, `insight`, `list`, `pantry`, `recipe`, `report`, `review`, `user`); modules own cross-cutting AI concerns — `generate` (the three recipe-generation providers, moved from the old `services/ai/`) and `language` (Ollama-based content moderation). Per-domain/module `constants.ts` holds select shapes and prompts (e.g. `domains/review/constants.ts`, `modules/language/constants.ts`).

**Agent-based language moderation (migrations `20260723100000`–`20260726010000`).** `Agent` (1:1 with `User`, created transactionally at registration via `createAgentForUser`/idempotently via `ensureAgentForUser` for OAuth) tracks a single rolling `language` verdict (`non_friendly|casual|formal`) — most-recent-overwrite across every checked content source. `modules/language/register.ts checkLanguage()` runs one of three presets (`LANGUAGE_REVIEW`, `LANGUAGE_FOLLOW`, `LANGUAGE_NOTE`) through a shared `callOllama()` (8s timeout, `qwen2.5:7b`, fails open to `formal`/approved on empty text, timeout, or any error). `LANGUAGE_REVIEW`/`LANGUAGE_FOLLOW` are `authority: "manual"`: moderation runs fire-and-forget after submission (`runReviewModeration`/`runFollowModeration`, called from `domains/review`/`domains/follow`) and only writes `languageVerdict`/`languageUpdatedAt` — it never touches `status`/`acceptedAt`/`rejectedAt`, which stay the recipe author's or followee's own decision, same separation-of-authority pattern as the existing flagging system. `GET /agents/me` exposes the caller's current verdict plus a display message resolved server-side from `LANGUAGE_CATEGORY` — clients never see the raw enum's meaning.

**CookRecord removed (`20260721120000` → `20260722100000`).** The STARTED/COMPLETED/ABANDONED cook-tracking model (added 2026-05-31) is gone: its use as a review-eligibility gate was dropped one day, the table itself the next. Review eligibility is now solely "recipe has a `shareToken`" (`domains/review/register.ts submitReview`). `20260722110000_drop_profile_visibility` also removed the unused profile-visibility JSON column that earlier entries flagged as scaffolding.

**Schema state.** 17 models as of `20260726010000_language_updated_at` (up from 15 on 2026-07-20): net +2, composed of +`Review`, +`ReviewCategoryRating`, +`Agent`, −`CookRecord`. `Review`/`ReviewCategoryRating` (migrations `20260721100000`–`130000`) is a from-scratch model distinct from the older simple-rating `RecipeReview` — both now coexist, `Review` carrying stars + per-category ratings (taste/difficulty/authentic) + a pending→accepted/rejected decision flow + one author reply (`response`/`respondedAt`), reusing the same re-submit-reuses-row pattern as `Follow`.
## ASSET:ARCHITECTURE 2026-07-20 07:12 ▸ Recipe/user flagging + admin moderation queue, username system, list color theming; topology/AI/routing/Redis/ops unchanged since 2026-07-13

Delta since the 2026-07-13 entry — topology, AI-provider abstraction, route versioning, Redis rate limits, Follow graph, timestamp-state user model, Visibility enum, and Slack/digest/Chat ops are unchanged below this entry.

**Flagging + moderation (migrations `20260715100000`–`110000`, `src/routes/reports.ts`, `src/routes/admin.ts`).** `RecipeReport`/`UserReport` are separate tables (different owners/cascades) sharing the same shape: `flagType` (`red`=report, `green`=endorse), optional `reason` (required for red, null for green), and a status cycle `pending → accepted/rejected → closed → (re-flag) → pending`. Re-flagging, including flipping red↔green, upserts the same row via the `[reporterId, recipeId]`/`[reporterId, targetId]` unique constraint and resets to pending — same reuse pattern as `Follow`. Self-report is blocked at the route level. Admin queues at `GET /admin/reports/{recipes,users}` (role-gated via `requireAdmin`) filter by status/flagType and decide via `PATCH .../:id/:decision(accept|reject|close)`, stamping `reviewedAt`/`reviewedBy`; `close` is only reachable from `accepted`/`rejected`.

**Username system (migrations `20260715120000`→`130000`, `src/lib/username.ts`).** Two-stage rollout: column added nullable, existing rows backfilled by a script run out-of-band (logged in `ts-toifood-dev`, not this repo), then locked `NOT NULL UNIQUE`. `generateUsername()` slugifies a seed (name or email) to ≤14 chars, appends 3-4 random digits, retries up to 20 times against the unique constraint before throwing. `USERNAME_REGEX` (app-layer only, no DB CHECK) requires letter+digit; a pure-letters username is reachable only via manual SQL, a deliberate escape hatch per the schema comment. All three registration paths (local, Google, Apple) in `src/routes/auth.ts` generate a username at account creation.

**List color theming (migration `20260719100000`, `src/routes/lists.ts`).** `ListColor` enum (`pro/free/admin/classic/creative/neutral/premium/basic/beta`) matches the frontend's `LabelVariant` theme set. New lists default to a random color if none is given; `PATCH` accepts a color-only update alongside (or instead of) rename. Schema now stands at 15 models as of `20260719100000_savedlist_color` (up from 13 on 2026-07-13, adding `RecipeReport`/`UserReport`).

**Recipe share tracking refined (migration `20260718100000`).** `shareToken` (permanent, `nanoid(10)`, set at generation time) is unconditionally public. `POST /recipes/:id/share` now also stamps `shareUpdatedAt` to record when the user last tapped the share button — distinct from the permanent token and from the row's general `updatedAt` — same nullable/not-backfilled convention as `visibilityUpdatedAt`/`User.preferencesUpdatedAt`.
## ASSET:ARCHITECTURE 2026-07-13 07:12 ▸ July 11–12 wave: Follow graph, timestamp-state user model, Visibility enum, auth-metrics pipeline, Google Chat ops

Delta since the 2026-07-06 entry — topology, AI-provider abstraction, route versioning, Redis rate limits, and Slack/digest ops are unchanged below this entry.

**Follow system (migration `20260711000000_add_follow_model`).** `Follow` is a timestamp-state machine — `acceptedAt` (pending → active), `unfollowedAt` (ended; re-follow reuses the unique `[followerId, followeeId]` row), `mutedAt`, `blockedAt` — no booleans. `src/routes/follows.ts` serves own and friend-view following/followers/pending lists; friend views are gated by `User.followVisibility` (`PATCH /follows/visibility` toggles it). Mounted at `/1-1-6/api/follows` + legacy `/follows`; `/users/me` now returns following/followers counts.

**Timestamp-state user model (migrations `20260711100000`–`130000`).** Booleans replaced by nullable timestamps on User: `premiumSince`/`premiumUntil` (active premium = `premiumUntil` in the future; `src/lib/premium.ts` is the single source of truth, admins always premium), `emailVerifiedAt`, `passwordSetAt` (null = OAuth-only). `20260711120000` backfilled `updatedAt` everywhere as drift cleanup. New `GET /admin/users` (admin-role guard) lists all users and derives linked providers `[google|apple|password]` from data rather than flags.

**Visibility enum (migrations `20260712100000`–`120000`).** `Visibility { private, public }` now gates `SavedList.visibility` and `User.followVisibility`, both defaulting `public` — chosen over share tokens after same-day iteration. Schema stands at 13 models as of `20260712120000_visibility_default_public`.

**Auth metrics pipeline.** Every non-local auth event is appended to `would/AUTH-METRIC.csv` (timestamp, event, method, userId, success, failReason, ip) and mirrored fire-and-forget to GitHub (`toifood-dev/ts-toifood-dev` via `TOIFOOD_CROSS_REPO_TOKEN`). Auth endpoints are rate-limited with `express-rate-limit` (10 req/15 min). Apple sign-in verifies tokens against a 1-hour-cached Apple JWKS.

**Google Chat ops.** `POST /chat` mirrors the Slack bot (`!status`/`!logs`/`!metrics` from pm2 and `would/*.csv`) as a Google Chat app endpoint; alerting also flows through the `chatAlert` webhook helper (`src/lib/chat.ts`). Client force-update floor `/app-config` `minVersion` now defaults to `1.1.9`. Claude provider (`src/services/ai/claude.ts`) uses `claude-haiku-4-5` with JSON-schema structured output (`claude-v5` prompt, 30 s timeout); dietary filters are enforced in the Claude prompt but deliberately omitted from Ollama prompts.
## ASSET:ARCHITECTURE 2026-07-06 07:25 ▸ Express+Prisma monolith on Mac mini M4 with pluggable AI providers, versioned routes, Redis rate limits, Slack/digest ops

**Topology.** Single Mac mini M4 behind a Cloudflare Tunnel (`toifood.co.nz`), split across two accounts: `jayreck` runs the Node.js API (:3000, PM2 `toifood-back`) and PostgreSQL 16 (:5432); `jayagent` runs Ollama (:11434, `qwen2.5:7b`), reached over `127.0.0.1`. Bootstrap is `scripts/macmini-setup.sh` (idempotent: Homebrew → Node 22 via nvm → PostgreSQL 16 → clone → `.env` with generated JWT_SECRET → `prisma migrate deploy` → PM2).

**Stack & decisions.** Node/TypeScript, Express 4, Prisma 5 + PostgreSQL, JWT auth (`src/middleware/auth.ts`) with local + Google/Apple sign-in, `trust proxy` for tunnel IPs, CORS allowlist via `CORS_ORIGIN`. AI generation sits behind an `AIProvider` interface (`src/services/ai/provider.ts`) with three implementations — Ollama (default; JSON-format prompt, 65s timeout, in-process request queue, CJK-bleed stripping, `ollama-v5` prompt version), OpenAI, Claude — selected by `AI_PROVIDER` (`src/services/ai/index.ts`). Region/continent is picked server-side from a curated `COUNTRY_REGIONS` table; emoji inferred from a keyword table.

**Route versioning (migration in progress).** All routers are mounted under `/1-1-6/` (`/1-1-6/auth`, `/1-1-6/api/*`, `/1-1-6/system/*`) with bare legacy paths kept alive for old app builds (`src/index.ts`). `/app-config` serves `minVersion` (default `1.1.6`) as the client force-update signal. Public `/stats` returns rounded counts with a 60s in-memory cache.

**Rate limiting.** Role-based daily caps in Redis (`src/middleware/rateLimit.ts`): free 3 ollama / 2 claude, premium 10/5, admin bypass. Atomic Lua INCR+EXPIRE (24h window); fails open with a warning if Redis is down. Usage surfaced via `getRecipeUsage`.

**Schema state (as of migration `20260703000000_remove_flows_model`).** 12 models: User (role enum `free|premium|admin` replaced `isPremium`; continent prefs, profile visibility JSON, age/gender added 2026-05-31), Recipe (share tokens, OG image bytes, provider/style/mealType/pantryUsed/cookTime/continent), RecipeReview, SavedList/SavedListItem (replaced Favourite, dropped 2026-04-14), DietaryPreference, PantryItem, UserInsight (history-style since `20260614`, status column removed `20260702`), CookRecord (STARTED/COMPLETED/ABANDONED lifecycle with ingredient/pantry/grocery snapshots, added 2026-05-31), password-reset + email-verification tokens. The flow system (added 2026-04-15) was fully removed 2026-07-03.

**Ops & observability.** Slack Socket-Mode bot (`src/slack-bot.ts`) in `#toifood-ops` answers `!status`/`!logs`/`!metrics` via PM2. Daily digest (`src/digest.ts`) aggregates per-provider recipe metrics (response time, pantry/grocery match %, fallbacks) from `would/*.csv` and posts to a Google Chat webhook, logging outcomes to `DIGEST-METRIC.csv`. Per-request logging middleware records method/path/status/latency/userId. Store metrics routes plus `src/services/appstore.ts`/`playstore.ts`/`youtube.ts` pull external channel data; `src/storeReport.ts` renders reports with `@napi-rs/canvas`.
