SHOULD ISSUE LOG
prompt: review and update ARCHITECTURE ISSUE decisions for 2026Q3
path: should/ARCHITECTURE-ISSUE-2026Q3.md
target: {repo}

INSTRUCTION FOR AI MODEL:

YOU MAY READ AND UPDATE EXISTING ENTRIES AS THE SYSTEM EVOLVES.
ADD NEW ENTRIES AT THE TOP FOR NEW TOPICS; UPDATE IN PLACE FOR EXISTING ONES.

FORMAT: ## ISSUE:{NAME} {YYYY-MM-DD HH:MM} → {CONTENT}

####### <!-- ANCHOR MARKER - ADD OR UPDATE ENTRIES DIRECTLY BELOW THIS LINE -->
## ISSUE:ARCHITECTURE 2026-09-07 08:18 ▸ README and referenced deployment docs have drifted far from actual implementation

**README.md is stale against the live API surface.** It documents a `/favourites` resource backed by a `Favourite`/`DietaryPreference` model that no longer exists in `prisma/schema.prisma` (favourites were superseded by `Bookmark` + `SavedList`), and omits entirely the `Follow`, `Review`, `RecipeReport`/`UserReport`, `Note`, `Draft`, `UserInsight`, `EmailPin`, and `Property` domains that make up most of `src/domains/*`. It also doesn't mention the `/1-1-6/...` versioned route namespace that `src/index.ts` mounts alongside every legacy unprefixed route, or the `ioredis` dependency (`package.json`) that `src/modules/rate` requires for tier-based rate limiting.

**Both docs linked from README are missing from the repo.** `README.md` points to `./docs/macmini-deployment.md` and `./docs/openclaw-integration.md`, but `repos/toifood/ts-toifood-back/contents/docs` returns 404 — there is no `docs/` directory at all. This is a concrete recovery gap: the README's own architecture section describes a bespoke two-macOS-account Mac mini deployment (Cloudflare Tunnel, colocated Postgres, loopback-only Ollama on a separate account) with no actual runbook backing it up. If the Mac mini needs to be rebuilt, there is currently no documented recovery procedure despite one being referenced as existing.

**`REDIS_URL` is absent from `.env.example`** even though `ioredis` is a non-optional dependency used by `src/modules/rate/register.ts` for all tier-based rate limiting (recipe generation, list/course creation, insight refresh, draft submission, email-pin issuance). A fresh setup following the documented `.env.example` + README env-var table has no indication Redis must be provisioned or how to point at it.

**Migration churn with no rollback/backfill record.** `prisma/migrations` shows several add-then-abandon cycles within the same quarter: `20260415023622_add_flow_system` → `20260703000000_remove_flows_model`; `20260531000000_add_cook_record` → `20260722100000_drop_cook_record`; `20260723100000_agent_table` → `20260810000000_retire_agent_table`; `20260828010000_personality_table` → `20260828020000_revert_personality_to_user_columns` (same day). None of these reversal migrations, nor any doc, records what happened to production data captured under the abandoned shape (e.g. `flow_system`/`cook_record`/`agent_table`/`personality_table` rows) before the drop — a recovery gap if any of that data was expected to survive the reversal.

**Permanent-looking route duplication with no deprecation tracking.** `src/index.ts` mounts every route module twice — once under `/1-1-6/...` and once unprefixed — with only a comment ("keep alive until old builds phase out") and no linked ticket or target removal date, risking indefinite duplication of the route surface.
## ISSUE:ARCHITECTURE 2026-08-31 08:49 ▸ Migration history can't rebuild a fresh database — `UserInsight` is ALTERed by `20260530000000` but never CREATEd by any migration, so `prisma migrate deploy` (the literal disaster-recovery command) would halt mid-replay on a truly empty Postgres instance

**NEW — confirmed independently, not just from the migration's own comment.** `20260828000000_property_type_own_enum`'s header states "the shadow-database replay this repo's migration history needs is currently broken (an older migration, `20260530000000_add_updated_at_drop_flowstep`, ALTERs `UserInsight` before any migration ever CREATEs it)." Verified by grepping all 95 `migration.sql` files for `CREATE TABLE "UserInsight"` — zero matches, including the `20260330025042_init` migration (which creates only `User`, `Recipe`, `Favourite`, `DietaryPreference`). `UserInsight` must have been created out-of-band (hand-run SQL or `prisma db push`, the same pattern already flagged for the Draft/Notification gap below) sometime before `20260530000000_add_updated_at_drop_flowstep` ran its `ALTER TABLE "UserInsight" ADD COLUMN "updatedAt"...`.

**This is strictly worse than the previously-flagged Draft/Notification gap — it breaks `migrate deploy` itself, not just a downstream API call.** `prisma migrate dev`/`migrate diff` needing a shadow database was already known to fail; the actual severity is that `npx prisma migrate deploy` — step 5 of `scripts/macmini-setup.sh`, the de-facto disaster-recovery runbook — applies migrations in strict order against the real target database with no shadow DB involved. On a genuinely fresh Postgres instance (hardware loss, new environment), replay would proceed cleanly through 90-odd migrations and then hard-fail at `20260530000000` with `relation "UserInsight" does not exist`, leaving the database partially migrated and the setup script's `pm2 start` never reached. Every environment that "works" today (production, and presumably any dev machine) only works because `UserInsight` was created by hand at some point outside the migration history and that database has carried it forward ever since — a from-scratch rebuild has never actually been exercised against this migration folder.

**Root cause of the repo's own repeatedly-flagged "hand-authored migrations" pattern, now admitted in-repo.** Every prior quarter's entries (2026-07-06 through 2026-08-24) noted same-day/next-day migration churn (visibility, opened-at, RecipeNote, and now Personality below) as if it were a style choice; `20260828000000` and `20260828010000`'s own comments now state plainly that hand-authoring is a *workaround* because `prisma migrate dev` can't run at all against this history. Fixing the `UserInsight` gap (e.g. a corrective no-op `CREATE TABLE IF NOT EXISTS "UserInsight"` migration inserted before `20260530000000`, or squashing history with a verified baseline) would very likely eliminate the underlying reason for most of the churn already on file, not just this one symptom.

**Draft/Notification migration gap — still unresolved, reverified this pass.** Grepped all 95 `migration.sql` files (up from 90 last check) for `CREATE TABLE "Draft"`/`CREATE TABLE "Notification"` and all four backing enum names — still zero matches, including all five migrations added since the 2026-08-24 entry (`20260826000000` through `20260828030000`, none of which touch either table). `POST /users/me/draft` remains live and routed. Same disaster-recovery exposure as before, now confirmed to be the second (not first) table-existence gap in this same migration history.

**Minor — `Personality` table created and reverted same day (`20260828010000` → `20260828020000`), no data loss.** Unlike the RecipeNote incident (2026-08-03/04, 87 rows discarded) or the continent-to-country drop (318 rows nulled), this round-trip backfilled `age`/`gender` into `Personality` rows and then copied them straight back to `User.ageRange`/`gender` before dropping the table — confirmed both migrations' `UPDATE`/`INSERT` statements are inverses. Flagging only as one more entry in the same-day-churn pattern, not as a data-integrity issue; the schema comment on the revert explains the reasoning (a future `matchUsers` should build on `Preference`, not a bespoke demographics table).

**Still open, unchanged since 2026-08-24 (reverified this pass):** `requireTier` (`src/modules/role/register.ts:44`) remains unwired — grep confirms it's referenced only in other modules' comments and its own test, no route imports it. `/chat` (`src/routes/chat.ts`) still has no auth import or check in the file. `.env.example` still has no `REDIS_URL`. `scripts/macmini-setup.sh` still clones `jayreck996/ts-toifood-back` instead of `toifood/ts-toifood-back`, still runs `pm2 start dist/index.js` against the real `dist/src/index.js` build output, still seeds `toifood_secret_change_me`, still writes `OLLAMA_MODEL=qwen2.5-claw:7b` against the code/README default of `qwen2.5:7b`. README still documents the retired `Favourite`/`/favourites` model and links to a nonexistent `docs/` directory. No PostgreSQL backup/restore procedure exists anywhere in the repo — now compounded by the fact that even a from-scratch restore-via-migration-replay is unproven and, per above, provably broken partway through.
## ISSUE:ARCHITECTURE 2026-08-24 06:35 ▸ Draft and Notification models exist in schema.prisma with zero backing migration — `POST /users/me/draft` is a live, routed endpoint that will throw on any fresh `prisma migrate deploy`

**NEW — `Draft` and `Notification` (plus their four enums `DraftType`/`DraftStatus`/`NotificationType`/`NotificationStatus`) were never created by any migration.** Checked systematically: grepped all 90 `migration.sql` files under `prisma/migrations/` for `CREATE TABLE "Draft"`, `CREATE TABLE "Notification"`, and all four enum names — zero matches anywhere in the committed history, including the most recent migration (`20260820010000_recipestyle_to_preferencestyle`). Both models are fully declared in `prisma/schema.prisma` (HEAD) with relations back to `User`.

**`Draft` is not dead scaffolding — it's wired end to end and reachable today.** `src/routes/users.ts` mounts `POST /users/me/draft`, `GET /users/me/draft`, `GET /users/me/draft/view/personal`, `GET /users/me/draft/view/system` (under both `/1-1-6/api/users` and the legacy `/users` prefix), all calling into `src/domains/draft/register.ts`. `submitDraft`/`runDraftModeration` call `prisma.draft.update(...)` directly, and on an approved language check also promotes the value into `User.name`/`username`/`bio`. This is the same class of drift the repo already caught once and documented in the migration comment for `20260817030000_add_userinsight_context` ("schema.prisma declared `UserInsight.context` but was never actually migrated against the database — caught via psql before restarting production, not after a crash") — except this time it's two entire tables backing a currently-mounted, rate-limited (`draftSubmitRateLimit`) user-facing feature, not one nullable column, and there's no comment anywhere recording that it was caught.

**Directly breaks the disaster-recovery path this log has flagged every quarter since 2026-07-06.** `scripts/macmini-setup.sh` step 5 runs `npx prisma migrate deploy` — confirmed still present, unchanged — which only replays the committed migration folder. On a from-scratch Mac mini rebuild (the exact scenario this script exists for), the rebuild would report success, the server would start, and the first call to `/users/me/draft` would fail with a Postgres "relation \"Draft\" does not exist" error, not a Prisma validation error, since the generated client trusts `schema.prisma` at build time regardless of what migrations ran. If the live server already has these tables from an out-of-band `prisma db push` or hand-run SQL (consistent with this repo's established pattern of hand-authoring migrations rather than using `prisma migrate dev`, already flagged for the 2026-07-12 and 2026-08-02 churn sequences), then production works today but any *new* environment — recovery, staging, a second contributor's machine — silently diverges from it.

**`Notification` carries no code risk today but is the same class of drift.** No file under `src/` calls `prisma.notification` anything — `modules/push/register.ts`/`constants.ts` only generate push copy strings (`CATEGORY_PUSH_RECIPE`/`CATEGORY_PUSH_INSIGHT`), there's no device-token table and no actual send path yet. Lower urgency than `Draft`, but worth fixing in the same pass — either commit the missing migration for both, or `prisma migrate dev` will generate one unprompted the next time anyone runs it locally and be confused why it's non-empty on an already-"up to date" branch.

**Still open, not reverified this pass (last confirmed 2026-08-10):** no PostgreSQL backup/restore procedure in the repo; `/chat` auth status; `requireTier` route wiring; Redis absence from `.env.example`/setup script. `scripts/macmini-setup.sh` and `README.md` were both re-checked this pass and are byte-for-byte unchanged from prior quarters — still clones `jayreck996/ts-toifood-back`, still `pm2 start dist/index.js` against real output `dist/src/index.js`, still seeds `toifood_secret_change_me`, still `OLLAMA_MODEL=qwen2.5-claw:7b`, README still documents the retired `Favourite`/`/favourites` model and links to a nonexistent `docs/` directory.
## ISSUE:ARCHITECTURE 2026-08-10 06:48 ▸ Aug 3–9 preference/note/list unification drops user data silently in two migrations; RecipeNote created then destroyed next day with 87 rows discarded; ListColor duplication persists (moved, not fixed)

**NEW — `20260805000000_unify_user_preferences` silently resets existing users' style/serving preferences with no backfill.** The migration folds `User.recipeStyle`/`continentPreferences`/`defaultServings` into the new `Preference` table but explicitly drops the three columns with "No backfill for style/continent/servings — dropped as-is" (migration comment). Any user who had previously set a non-default `recipeStyle` or `defaultServings` (both were plain `User` columns, not join-table rows) loses that value with no error and no notice — `modules/preference/register.ts getUserPreferences()` just falls back to the module defaults (`classic` / `2`) for every user until they next hit `PATCH /users/me/preferences`. Functionally safe (no crash, no orphaned FK), but it's a real, silent preference reset shipped as a side effect of a refactor migration, not a product decision documented anywhere in the migration comment the way the continent-drop below is.

**NEW — `20260808000000_continent_to_country` nulls 318 live `Recipe.country` values with no way back.** Confirmed by the migration's own pre-flight comment: 0 `Preference` rows had `type='continent'` (no loss there), but 318 `Recipe` rows had a continent value ("Asia", etc.) that isn't a valid country under the new 65-demonym taxonomy (`modules/preference/constants.ts` `COUNTRY_FLAGS`), so they're set to `NULL` outright. Product-accepted ("purely a cosmetic loss" per the migration comment) — flagging because it's the second data-dropping migration in 3 days (see above), and the SEO `recipeCuisine` tag / public profile country-breakdown stat for those 318 recipes is now silently blank rather than wrong-but-present.

**NEW — `RecipeNote` created 2026-08-03, destroyed 2026-08-04, 87 rows discarded.** `20260803010000_note_list_lifecycle` creates `RecipeNote` (per-recipe description/ingredient/method notes with the new boundary-timestamp shape) and backfills it from `Recipe`'s old flat note columns. The very next migration, `20260804010000_retire_recipenote_for_note`, drops the table entirely in favor of `Note.recipeId`/`type`, with the comment "Existing RecipeNote data (87 active rows) discarded per explicit product decision, not migrated." This is the same same-day/next-day hand-authored-migration churn already flagged for the 2026-07-12 visibility and 2026-08-02 opened-at sequences, except this instance actually destroys 87 rows of real (if short-lived) data rather than just renaming/re-adding columns — worth distinguishing from the purely-cosmetic churn pattern already on file.

**Confirmed still unresolved, just relocated — `ListColor` enum duplication.** The 2026-07-20 entry flagged `src/routes/lists.ts` hand-duplicating `ListColor` as a local array. Post thin-controller refactor, the duplication is now `LIST_COLORS` in `src/domains/list/constants.ts:786` — same nine hardcoded string literals, still not imported from Prisma's generated `ListColor` enum. Moved, not fixed; still two sources of truth that can silently diverge.

**Minor — stale comment references a model removed over a month ago.** `src/domains/user/register.ts`'s `deleteAccount` comment says "User cascades → SavedList, PantryItem, UserFlowView" — `UserFlowView` was part of the flow system removed in migration `20260703000000_remove_flows_model`. No functional impact (the actual cascades — `Follow`, `Review`, `Bookmark`, `Note`, `RecipeReport`, `UserReport`, `SavedList`, `PantryItem` — are all correctly declared `onDelete: Cascade` in `schema.prisma`; `Preference`/`EmailPin` lack cascade and are correctly deleted manually first), just a documentation trail leftover.

**Still open, unchanged since 2026-08-03:** `requireTier` remains unwired to any route. `/chat` remains unauthenticated. No Redis in `scripts/macmini-setup.sh`/`.env.example`. Setup script still targets `pm2 start dist/index.js` against the real `dist/src/index.js` build output, still clones `jayreck996/ts-toifood-back` instead of `toifood/ts-toifood-back`, still seeds `toifood_secret_change_me` as the live DB password, and its generated `.env` still writes `OLLAMA_MODEL=qwen2.5-claw:7b` against the code/README default of `qwen2.5:7b`. README still links to a nonexistent `docs/` directory and still documents the retired `Favourite`/`/favourites` model instead of the current 18-model schema. No PostgreSQL backup/restore procedure exists anywhere in the repo — single Mac mini SPOF remains the top unaddressed recovery item.
## ISSUE:ARCHITECTURE 2026-08-03 07:18 ▸ Aug-2 opened-at migration churn leaves orphaned senderOpenedAt/receiverOpenedAt columns undeclared in schema.prisma; requireTier ships unwired; recovery/ops gaps unchanged

**NEW — the 2026-08-02 opened-at migration sequence ends in schema drift.** Five migrations landed on 2026-08-02 touching `Follow`/`Review` "last opened" tracking: `20260802000000_add_opened_at` adds `selfOpenedAt`/`friendOpenedAt`; `20260802000001_drop_sender_receiver_opened_at` drops (IF EXISTS) an older `senderOpenedAt`/`receiverOpenedAt` pair; `20260802010000_review_follow_opened_at` adds a single `openedAt`; `20260802020000_review_follow_sender_receiver_opened_at` drops `openedAt` and **re-adds** `senderOpenedAt`/`receiverOpenedAt` on both tables. Applied in order, the final Postgres state has *four* opened-at columns per table (`selfOpenedAt`, `friendOpenedAt`, `senderOpenedAt`, `receiverOpenedAt`) — but `prisma/schema.prisma` (HEAD) only declares `selfOpenedAt`/`friendOpenedAt` on `Follow` and `Review`, and `src/domains/follow/register.ts`/`src/domains/review/register.ts` only ever read/write those two. `senderOpenedAt`/`receiverOpenedAt` exist in the live schema with no Prisma Client mapping and no code path — dead columns that `prisma migrate dev`/`migrate diff` would flag as drift against a Prisma-generated migration history. Also worth noting: `20260802000000_add_opened_at` and `20260802000000_review_drop_stars` share the identical `20260802000000` timestamp prefix — migrations here are being hand-authored rather than generated via `prisma migrate dev`, the same pattern already flagged for the 2026-07-12 visibility churn.

**NEW — `requireTier` (`src/modules/role/register.ts:42`) ships unwired.** GitHub code search confirms it's imported only by its own test (`src/__tests__/role.test.ts`) — no route file imports or mounts it, unlike `requireAdmin` which gates `src/routes/admin.ts` and `src/routes/storeMetrics.ts`. The minimum-tier gate exists and is unit-tested but nothing in the API actually restricts a pro/super_pro-only feature yet. Same "shipped unwired" pattern as `LANGUAGE_NOTE` (flagged 2026-07-27).

**Still open, unchanged since 2026-07-27:** `/chat` (`src/routes/chat.ts`) remains unauthenticated. `scripts/macmini-setup.sh` still installs no Redis (no `REDIS_URL` in `.env.example` either) despite `src/modules/rate/register.ts`'s rate limiter failing open with only a console warning on Redis failure — confirmed unchanged this pass. Setup script still clones `jayreck996/ts-toifood-back` instead of `toifood/ts-toifood-back`, still targets `pm2 start dist/index.js` against the real `dist/src/index.js` build output, and still seeds `toifood_secret_change_me` as the live DB password. README still links to a nonexistent `docs/` directory. `.env.example`'s GitHub-target comment (`toifood/-ts-toifood-dev`) still doesn't match the code constant (`src/routes/auth.ts:18`, now `toifood/ts-toifood-dev` — confirmed still mismatched, no change from the 2026-07-27 correction). No PostgreSQL backup/restore procedure exists anywhere in the repo — single Mac mini SPOF remains the top unaddressed recovery item this quarter.
## ISSUE:ARCHITECTURE 2026-07-27 07:18 ▸ storeReport.ts writes to archive paths that don't exist; language moderation adds a third unqueued Ollama caller; LANGUAGE_NOTE preset shipped unwired; recovery/docs/chat gaps unchanged

**NEW — `src/storeReport.ts` writes to archive paths that don't exist.** `ISSUE_PATH`/`ASSET_PATH` are hardcoded to `-ARCHIVE/-WOULD/usage-issue-v1.md` / `usage-asset-v1.md` (src/storeReport.ts:19-20). The actual repo tree has `archive/` (no leading dash) and `would/` (no `-WOULD` subdirectory under archive) — and even `archive/could/`'s own files have moved to the quarterly `USAGE-ISSUE-2026Q2.md` convention, not `usage-issue-v1.md`. `prependEntry()` calls `fs.readFileSync(filePath)` with no existence check, so any run of this script throws `ENOENT` before writing anything. Nothing in the repo (no package.json script, no `.yml`/ecosystem/crontab file) actually invokes `storeReport.ts` — it may be dead code, but if something external (e.g. a Mac mini cron) calls it, the weekly store KPI report is currently broken.

**NEW — language moderation adds a third independent, unqueued Ollama caller.** `modules/language/utils.ts callOllama()` posts directly to `${OLLAMA_BASE_URL}/api/generate`, separate from `OllamaProvider`'s serialized in-process promise queue (`modules/generate/register.ts`) and `digest.ts`'s `ollamaChat` (`/api/chat`). All three hit the same single local `qwen2.5:7b` model with no shared concurrency control. Moderation fires fire-and-forget on every review and follow-request submission with only an 8s timeout (`OLLAMA_TIMEOUT_MS`) before failing open to `formal`/approved — under recipe-generation load on the same model, moderation checks will silently start failing open rather than queuing, quietly disabling the non-friendly flag exactly when the server is busiest. Same anti-pattern already flagged for Redis in the 2026Q2 log ("Two independent Redis connections") — Ollama now has three uncoordinated callers.

**NEW — `LANGUAGE_NOTE` preset shipped unwired.** `modules/language/constants.ts` defines `LANGUAGE_NOTE` (`authority: "auto"` — the only preset meant to self-gate content rather than just annotate it) with a prompt explicitly written for `Recipe.descriptionNote`/`ingredientNote`/`methodNote`. No route or domain file imports `LANGUAGE_NOTE` or calls `checkLanguage(LANGUAGE_NOTE, ...)` anywhere — those three note fields ship with zero moderation despite the enforcement logic already being written and exported.

**Correction to the 2026-07-13 entry.** The auth-metrics GitHub target (`src/routes/auth.ts:17`, `GITHUB_OUTPUT_REPO`) has since changed from `toifood-dev/ts-toifood-dev` to `toifood/ts-toifood-dev`, but `.env.example`'s comment still says `toifood/-ts-toifood-dev` (dash-prefixed) — the mismatch persists with different specifics than before.

**Minor.** `20260722100000_drop_cook_record` removed the `CookRecord` model entirely (STARTED/COMPLETED/ABANDONED lifecycle, added 2026-05-31) the day after `20260721120000_review_drop_cookrecord_gate` dropped its use as a review-eligibility gate — clean two-step removal, no lingering references found in `src/domains/review` or `src/routes/reviews.ts`.

**Still open, unchanged:** `/chat` (`src/routes/chat.ts`) has no auth check of any kind — unauthenticated `!logs` still returns PM2 log lines containing per-request userIds, `!status` still exposes process topology. Redis remains absent from `scripts/macmini-setup.sh` and `.env.example` (no `REDIS_URL`); rate limiting still fails open on Redis failure. `scripts/macmini-setup.sh` still runs `pm2 start dist/index.js` against a `dist/src/index.js` build output (confirmed again via `tsconfig.json`'s `rootDir: "."` + `include: ["src"]`), still clones `jayreck996/ts-toifood-back` instead of `toifood/ts-toifood-back`, and still seeds `toifood_secret_change_me` as the live DB password. README still links to a nonexistent `docs/` directory (`docs/macmini-deployment.md`, `docs/openclaw-integration.md` both 404). No PostgreSQL backup/restore procedure exists anywhere in the repo — single Mac mini SPOF remains the top unaddressed recovery item.
## ISSUE:ARCHITECTURE 2026-07-20 07:12 ▸ Disaster-recovery script starts a nonexistent file; README links to a docs/ dir that doesn't exist; shared-types drift widens again; visibility churn resolved but /chat and auth-metrics gaps unchanged

**NEW — `scripts/macmini-setup.sh` runs `pm2 start dist/index.js`, but the build output is `dist/src/index.js`.** `tsconfig.json` sets `rootDir: "."` with `include: ["src"]`, so `tsc` preserves the `src/` prefix in `dist/` — confirmed by `package.json`'s own `"start": "node dist/src/index.js"` and by README's deploy step (`pm2 start dist/src/index.js`). This script is the de-facto disaster-recovery runbook for the single-Mac-mini deployment: replaying it after hardware loss would build successfully, then silently fail to start the process (pm2 would just have no running `toifood-back` app, with no error surfaced beyond that).

**NEW — README links to a `docs/` directory that doesn't exist in the repo.** `docs/macmini-deployment.md` and `docs/openclaw-integration.md` (README.md lines 24-25) both 404 against the `main` tree. As written, the "full Mac mini setup guide" and "how the local AI model connects to the backend" docs — exactly what a recovery operator would reach for first — aren't there.

**RESOLVED — Recipe visibility churn** (was flagged 2026-07-13 as a stub left by the share-token→Visibility migration). `20260717100000` added `Recipe.visibility`, `20260717110000` added a matching timestamp column — then `20260717120000` dropped both the same week, with the migration comment recording that recipes are permanently public by design and `SavedList.visibility` is the only real privacy control. `20260718100000` added `Recipe.shareUpdatedAt` instead (tracks share-button taps, separate from the permanent `shareToken`). No action needed; the design question is settled.

**Shared contract drift widened again.** Since the 2026-07-13 entry, three more fields shipped with no `shared/src/index.ts` counterpart: `User.username` (now `NOT NULL UNIQUE`, migrations `20260715120000`/`130000`), `SavedList.color` (`ListColor` enum, migration `20260719100000`), `Recipe.shareUpdatedAt`. `shared/src/index.ts`'s `User` interface still has no `username` field at all — clients relying on the shared type are coding against a `User` shape that's now three fields behind the real API response, on top of the already-flagged `isPremium`/`emailVerified`/`hasPassword` booleans and dead `Favourite` interface.

**Still open, unchanged:** `/chat` (`src/routes/chat.ts`) has no auth check of any kind — still reachable by anyone who can hit toifood.co.nz, still returns PM2 logs with per-request userIds via `!logs`. Auth-metrics GitHub target mismatch unresolved: the code constant (`src/routes/auth.ts:23`, `GITHUB_OUTPUT_REPO`) is `toifood-dev/ts-toifood-dev`, while `.env.example`'s comment says `toifood/-ts-toifood-dev` — still unclear which is correct. Redis is still absent from `scripts/macmini-setup.sh` and `.env.example` (no `REDIS_URL`); rate limiting still fails open on Redis failure. Setup script still clones `jayreck996/ts-toifood-back` instead of `toifood/ts-toifood-back` and still seeds `toifood_secret_change_me` as the live DB password. No PostgreSQL backup/restore procedure exists anywhere in the repo.

**Minor:** `src/routes/lists.ts` hand-duplicates the `ListColor` enum values as a local `LIST_COLORS` const array instead of importing Prisma's generated enum — two sources of truth that can silently diverge if the schema enum ever changes.
## ISSUE:ARCHITECTURE 2026-07-13 07:12 ▸ Unauthenticated ops endpoint leaks logs, auth PII pushed to GitHub, shared-types drift widened by July 11–12 migrations; SPOF & Redis gaps still open

**NEW — `/chat` is an unauthenticated remote-ops endpoint.** `src/routes/chat.ts` is mounted at both `/chat` and `/1-1-6/api/chat` with no `requireAuth` and no verification that requests actually come from Google Chat (no bearer/ID-token check). Anyone who can reach toifood.co.nz can POST `!logs` and receive the last 20 PM2 log lines (which contain per-request userIds from the logging middleware), or `!status` for process topology. Verify Google Chat's Authorization token or require a shared secret before answering.

**NEW — auth metrics push PII to GitHub via race-prone whole-file rewrites.** `src/routes/auth.ts` appends every non-local auth event (userId, IP, fail reason) to `would/AUTH-METRIC.csv` and mirrors it fire-and-forget to GitHub via GET-full-file → append one row → PUT, once per event (single 409 retry). Concurrent auth events can drop rows (last-writer-wins), each event re-uploads the entire ever-growing CSV, and IP+userId in a git repo's permanent history is a privacy/deletion liability. Also `.env.example` documents the target as `toifood/-ts-toifood-dev` while the code constant is `toifood-dev/ts-toifood-dev` — one of them is wrong.

**Shared contract drift got worse, not better.** The July 11–12 migrations moved User to timestamp-state (`premiumSince`/`premiumUntil`, `emailVerifiedAt`, `passwordSetAt`, `followVisibility`) and `/users/me` now returns those plus follower counts — but `shared/src/index.ts` still exposes `isPremium: boolean`, `emailVerified: boolean`, `hasPassword: boolean`, a dead `Favourite` interface, and no Follow/Visibility types at all. README likewise still documents `/favourites` and predates `/follows`, the `/1-1-6/` prefixes, and the visibility system.

**Share-token → visibility churn left a stub.** Three migrations on 2026-07-12 (`share_tokens_list_follow` → `visibility_enum_replaces_share_tokens` → `visibility_default_public`) replaced list/follow share tokens with the `Visibility` enum within one day. `Recipe.shareToken`/`ogImage` remain from the older per-recipe share design — decide whether recipe sharing also moves to `Visibility`, or the churn repeats.

**Still open from 2026-07-06 (unchanged):** single Mac mini SPOF with no `pg_dump`/off-machine backup; rate limiting depends on Redis (`src/middleware/rateLimit.ts`, fails open) yet Redis is absent from `scripts/macmini-setup.sh` and `.env.example` has no `REDIS_URL`; setup script still clones `jayreck996/ts-toifood-back` instead of `toifood/ts-toifood-back` and seeds the `toifood_secret_change_me` DB password; legacy bare routes still mounted with no traffic measurement or removal criterion — client force-update floor is now `1.1.9` while the API prefix is still `/1-1-6/`, so the version signals are drifting apart. Correction to the 2026-07-06 entry: `express-rate-limit` is no longer unused — `src/routes/auth.ts` applies it (10 req/15 min) to auth endpoints.
## ISSUE:ARCHITECTURE 2026-07-06 07:25 ▸ Single-host SPOF, Redis missing from bootstrap, README/shared-types drift behind July schema

**Single point of failure — no recovery path documented.** The entire stack (Node :3000, PostgreSQL :5432, Ollama :11434, Cloudflare Tunnel) runs on one Mac mini M4. There is no PostgreSQL backup/restore procedure anywhere in the repo (`scripts/macmini-setup.sh` provisions but never schedules `pg_dump`), and metrics live as unrotated CSVs on local disk (`would/RECIPE-METRIC.csv`, `DISCOVER-METRIC.csv`, `DIGEST-METRIC.csv` — see `src/digest.ts`, `src/slack-bot.ts`). Machine loss = total data loss of users, recipes, and metrics. A backup cron + off-machine copy should be the top recovery item this quarter.

**Rate limiting depends on Redis, but Redis is not provisioned.** `src/middleware/rateLimit.ts` enforces role-based daily caps (free: 3 ollama / 2 claude) via ioredis with an atomic Lua INCR+EXPIRE, and on Redis failure it logs a warning and **skips the limit**. `scripts/macmini-setup.sh` installs Homebrew, Node 22, PostgreSQL 16, and PM2 — never Redis. On a fresh machine rebuild, free users get unlimited Claude (paid API) generations silently. Either add `brew install redis` to the setup script or fail closed for the `claude` provider.

**Setup script drift vs. reality.** `scripts/macmini-setup.sh` clones `https://github.com/jayreck996/ts-toifood-back.git` while the canonical repo is `toifood/ts-toifood-back`; it writes `OLLAMA_MODEL=qwen2.5-claw:7b` while code default and README say `qwen2.5:7b`; and it seeds `toifood_secret_change_me` as the live DB password with only an echo reminder. The script is the de-facto disaster-recovery runbook, so this drift breaks recovery exactly when it's needed.

**README documents a schema that no longer exists.** README still lists the `Favourite` model and `/favourites` routes (table dropped in migration `20260414000000_remove_favourite_table`, replaced by `SavedList`/`SavedListItem`), omits the `/1-1-6/` versioned prefixes, and omits chat/insights/lists/records/store-metrics routes entirely. Likewise `shared/src/index.ts` `User` still exposes `isPremium: boolean` while `prisma/schema.prisma` moved to `role: UserRole (free|premium|admin)` — the shared contract with the mobile app lags the schema.

**Legacy route migration has no end date.** `src/index.ts` mounts every router twice (`/1-1-6/...` and bare legacy paths) "until old builds phase out", but nothing measures legacy traffic to decide when. The request logger already captures paths — aggregate it and set a removal criterion, or the duplication becomes permanent.

**Scale-up hazards baked into single-process assumptions.** `OllamaProvider` serializes all generations through an in-process promise queue (`src/services/ai/ollama.ts`) and `/stats` uses an in-memory 60s cache (`src/index.ts`); both silently break under PM2 cluster mode. Also `uncaughtException` is logged but the process keeps running in unknown state — prefer log-then-exit and let PM2 restart. Minor: committed build artifact `shared/src/index.js`; `express-rate-limit` dependency appears unused now that limiting is custom Redis; `@types/*` packages sit in `dependencies` instead of `devDependencies`; Prisma pinned at 5.14 (6.x is current).
