MUST ISSUE LOG
prompt: review and update ROADMAP ISSUE compliance and business requirements for 2026Q3
path: must/ROADMAP-ISSUE-2026Q3.md
target: {repo}

INSTRUCTION FOR AI MODEL:

YOU MAY READ AND UPDATE EXISTING ENTRIES AS REQUIREMENTS EVOLVE.
ADD NEW ENTRIES AT THE TOP FOR NEW TOPICS; UPDATE IN PLACE FOR EXISTING ONES.

FORMAT: ## ISSUE:{NAME} {YYYY-MM-DD HH:MM} → {CONTENT}

####### <!-- ANCHOR MARKER - ADD OR UPDATE ENTRIES DIRECTLY BELOW THIS LINE -->
## ISSUE:roadmap 2026-09-07 08:01 ▸ matchUsers' candidate pool is gated to a tier (super_pro) with no grant path, so real users get empty match results; same-day Personality table add-then-revert

New gap found this pass, plus a status check on all previously logged items. (1) New: `matchUsers` (`src/domains/user/register.ts:537-575`, mounted at `GET /users/view/self/matches` per `CATEGORY_API_USER.matchUsers` in `src/modules/api/constants.ts:197`) self-joins `Preference` on shared (type, value) pairs weighted by global rarity, but restricts the candidate side to `u2.role IN ('super_pro', 'admin')` (line 552) — the same asymmetric-restriction pattern as `matchLists`' own `sl2.type = 'course'` gate (`src/domains/list/register.ts`). Because the carried premium-grant-path gap below means no real user can ever reach `super_pro` through the app itself, every caller's candidate pool is limited to whichever accounts an operator has manually flipped to `super_pro`/`admin` via direct DB edit. For a deployment with zero manually-promoted users, `GET /users/view/self/matches` returns `{ matches: [] }` unconditionally regardless of how many `Preference` rows overlap between real users — the query and route are fully implemented and reachable by any authenticated caller, but structurally starved by the same unshipped monetization path already tracked here. (2) Governance note, not a gap: migration `20260828010000_personality_table` added a `Personality` table (per-user `age`/`gender` rows mirroring `Property`'s shape, meant to feed a demographics-based match variant) and backfilled `User.ageRange`/`gender` into it; `20260828020000_revert_personality_to_user_columns`, applied the same day, restored the two columns and dropped the table again, reasoning in its own comment that `Preference` already covers the matching use case better than raw demographics. Both migrations reached `main` — net schema effect is a no-op — and it's the second same-day create/revert in this repo's migration history (the first being `FlowStep`, per this revert's own comment). (3) Carried, still open, now with a second live feature depending on it: no billing/subscription/IAP/receipt route anywhere in the codebase; `GET /admin/users` (`src/routes/admin.ts:11-41`) remains read-only — `role`/`premiumSince`/`premiumUntil` (lines 18-19) are only ever selected, never written, so premium (and now match-visibility) stays settable only by direct DB edit. (4) Carried, still open: `Follow.mutedAt`/`blockedAt` (`prisma/schema.prisma`) remain schema-only — `src/domains/follow/register.ts` only ever reads `blockedAt` (lines 96, 290, 313, 368), no route in `src/routes/follows.ts` sets or clears either field. (5) Carried, still open: `src/modules/playstore/register.ts:696-698` still hardcodes `installs30d`/`activeDevices30d` to `null` pending a BigQuery export from Play Console. (6) Carried, still open: version-surface drift — routes still mounted under `/1-1-6/*` (`src/index.ts:60-80`) while `/app-config`'s `minVersion` fallback is `"1.1.9"` (`src/index.ts:135`), no `/1-1-9` mount added. (7) Carried, still open: `src/routes/insights.ts` still exposes only `GET /insights` and `POST /insights/refresh` (whole-set regenerate), no per-suggestion accept/dismiss endpoint despite `UserInsight`'s lifecycle-shaped schema supporting one.
## ISSUE:roadmap 2026-08-31 08:31 ▸ Pro/Super Pro subscription tiers are fully modeled and promoted in-product, but no code path ever grants or revokes them

`prisma/schema.prisma` defines `User.role` (`UserRole`: free/pro/super_pro/admin) plus `premiumSince`/`premiumUntil` timestamps, and `src/modules/rate/constants.ts` gates real limits off that tier (`CATEGORY_RATE_RECIPE`, `CATEGORY_RATE_LIST`, `CATEGORY_RATE_INSIGHT`, `CATEGORY_RATE_DRAFT`, `CATEGORY_RATE_LIST_COURSE`) with client-facing copy like "Upgrade to Pro or Super Pro for higher daily limits" and "Manage or cancel your subscription anytime from Settings" (`CATEGORY_RATE_INSIGHT.pro.help`).

Despite that surface, nothing in the backend ever changes a user's `role`, `premiumSince`, or `premiumUntil`:
- `src/routes/admin.ts` (`GET /admin/users`) only *reads* `role`/`premiumSince`/`premiumUntil` — there is no PATCH/POST admin endpoint to set them.
- `src/domains/user/register.ts` only reads these fields (lines 20-21, 73-74, 426, 567); no write site exists anywhere in the tree.
- `src/modules/appstore/register.ts` and `src/modules/playstore/register.ts` only pull App Store/Play Store install/crash analytics — neither validates IAP receipts nor handles purchase webhooks.
- `package.json` has no Stripe, Apple receipt-verification, or Google Play Billing dependency, and there is no `/billing`, `/subscribe`, or App Store Server Notifications / Google Play RTDN webhook route under `src/routes`.

Net effect: a real user can never become `pro`/`super_pro` through the app itself — the only way today is a manual DB write. The product copy promises an upgrade/cancel flow that doesn't exist server-side.
## ISSUE:ROADMAP 2026-08-24 06:41 ▸ OpenAIProvider gap corrected — dead code, never reachable in production; premium billing, follow block/mute, Play Store metrics, and insight actions all carried unchanged

Correction to the 2026-08-10 log plus a status check on all previously logged items. (1) Correction: `getAIProvider()`/`OpenAIProvider` (`src/services/ai/index.ts`, `src/services/ai/openai.ts`) are unreferenced anywhere else in the codebase — a full-source grep for `OpenAIProvider`/`services/ai` turns up only those two files importing each other. The live recipe-generation path is `generateRecipe()` in `src/modules/generate/register.ts` (lines 305-320), which only ever tries Claude (`requestedProvider === "claude"` with `ANTHROPIC_API_KEY` set) falling back to Ollama, or goes straight to Ollama — `OpenAIProvider` is never instantiated by any route. This is empirically confirmed by migration `20260818010000_add_recipe_provider_enum`'s own comment: live `Recipe.provider` data was verified as "323 claude, 107 ollama, 2 null, no stray values" before the enum was added — zero `openai` rows exist. The 2026-08-10 log's claim that "`AI_PROVIDER=openai` is a fully supported third provider option alongside ollama/claude" with real user-facing impact from dropped country/method preferences was inaccurate — that code path is unreachable in production, so no live request is affected; the dropped-preference bug remains real but inert dead code, not a shipping gap. (2) Carried, still open: no billing/subscription/IAP/receipt route anywhere in the codebase (grepped full source for `receipt|webhook|subscription|iap|purchase|entitlement` — zero matches); `GET /admin/users` (`src/routes/admin.ts`) remains read-only with no role-change endpoint — premium is still only settable by direct DB edit, now against three paid tiers with per-tier rate/list/insight presets fully priced out in `src/modules/rate/constants.ts`. Seven logs running. (3) Carried, still open: `Follow.mutedAt`/`blockedAt` (`prisma/schema.prisma`) remain schema-only — `src/domains/follow/register.ts` only ever reads `blockedAt` (lines 96, 290, 313, 368), no route in `src/routes/follows.ts` sets or clears either field. Seven logs running. (4) Carried, still open: `src/modules/playstore/register.ts` still hardcodes `installs30d`/`activeDevices30d` to `null` pending a BigQuery export from Play Console. (5) Carried, still open: version-surface drift — routes still mounted under `/1-1-6/*` (`src/index.ts:60-78`) while `/app-config`'s `minVersion` fallback is `"1.1.9"` (`src/index.ts:135`), no `/1-1-9` mount added. (6) Carried, still open, now with more schema behind it: `UserInsight` gained a full lifecycle shape this quarter (`status`/`requestedAt`/`acceptedAt`/`rejectedAt`/`removedAt`, migration `20260817010000_userinsight_slot_shape`) mirroring Note/Draft, but `status` here still only tracks internal generation success vs. fallback-to-template (`getInsights` filters `status: "accepted"` meaning "analysis completed", not a user decision — `src/domains/insight/register.ts`) — `src/routes/insights.ts` still exposes only `GET /insights` and `POST /insights/refresh` (whole-set regenerate), no per-suggestion accept/dismiss endpoint despite the schema now having the shape to support one.
## ISSUE:ROADMAP 2026-08-10 06:38 ▸ OpenAIProvider silently drops country and method preferences (method just shipped 2026-08-09); five carried gaps unchanged

New gap found this pass, plus a status check on all previously logged items. (1) New: `src/services/ai/openai.ts`'s `OpenAIProvider.generateRecipe` hand-builds its prompt independently of the `GENERATE_RECIPE_BASIC`/`GENERATE_RECIPE_PREMIUM` preset system (`src/modules/generate/constants.ts`) that Ollama/Claude both go through — it reads `request.dietaryFilters` directly into the prompt, but never reads `request.countryPreferences` or `request.methodPreferences` (`contracts.ts:104-113`) at all. Since `AI_PROVIDER=openai` (`src/services/ai/index.ts`) is a fully supported third provider option alongside ollama/claude, any deployment running it silently ignores a user's country preference (`Preference.type='country'`) entirely and — more notably — the cooking-method preference axis added just one day earlier by migration `20260809000000_add_preference_method` (`src/modules/preference/constants.ts` `METHODS`/`PREFERENCE_METHOD_CATEGORY`) never reaches the model under this provider, unlike the documented Ollama/Claude split where `honorsCountry`/`honorsMethod` flags at least make the gating explicit and user-facing copy (`PREFERENCE_SECTIONS.method.info`, "Only applies with the premium (Claude) provider") discloses it. OpenAI's silent drop has no equivalent disclosure. (2) Carried, still open: no billing/subscription/IAP/receipt route anywhere in the codebase; `GET /admin/users` (`src/routes/admin.ts`) remains read-only with no role-change endpoint — premium is still only settable by direct DB edit, now against three paid tiers (free/pro/super_pro). Six logs running. (3) Carried, still open: `Follow.mutedAt`/`blockedAt` (`prisma/schema.prisma`) remain schema-only — `src/domains/follow/register.ts` only ever reads `blockedAt` (lines 96, 290, 313, 368), no route in `src/routes/follows.ts` sets or clears either field. Six logs running. (4) Carried, still open: `src/modules/playstore/register.ts` still hardcodes `installs30d`/`activeDevices30d` to `null` pending a BigQuery export from Play Console. (5) Carried, still open: version-surface drift — routes still mounted under `/1-1-6/*` (`src/index.ts`) while `/app-config`'s `minVersion` fallback is `"1.1.9"` (`src/index.ts:132`), no `/1-1-9` mount added. (6) Carried, still open: insight actionability — `src/routes/insights.ts` still exposes only `GET /insights` and `POST /insights/refresh` (whole-set regenerate), no per-suggestion accept/dismiss endpoint.
## ISSUE:ROADMAP 2026-08-03 07:05 ▸ Premium tier lineup expands to three (free/pro/super_pro) with zero grant path; follow block/mute still unreachable; corrects prior "orphaned RecipeReview" claim

Reviewed against the 2026-07-27 log. (1) Premium/billing gap widens rather than closes: migration `20260728000000_user_role_super_pro` added a third paid tier — `UserRole` (`prisma/schema.prisma`) is now `free | pro | super_pro | admin`, and `src/modules/rate/constants.ts` prices out daily generation quotas per tier (`RATE_PRO` ollama 10/claude 5, `RATE_SUPER_PRO` ollama 30/claude 15) — but there is still no billing/subscription/IAP/receipt route anywhere in the codebase, and `src/routes/admin.ts` `GET /admin/users` remains read-only with no role-change endpoint. Five logs running with the same core gap, now against three paid tiers instead of one. (2) Correction to the 2026-07-27 log: that entry's claim that the legacy `RecipeReview` model "has zero route surface anywhere in `src/routes/*.ts`" is inaccurate — `POST /recipes/:id/review` (`src/routes/recipes.ts` → `reviewRecipe` in `src/domains/recipe/register.ts`) actively upserts `RecipeReview` rows, and `discoverRecipes`'s raw SQL (same file, ~line 457) LEFT JOINs `RecipeReview` to compute `avgStars`/`reviewCount`/`myStars` for the discover feed. The two review systems are both live and intentionally distinct — `RecipeReview` is a lightweight star-rating signal feeding discovery ranking, while `Review` (`src/domains/review/register.ts`) is the moderated, categorized review with an accept/reject/response workflow — not orphaned schema. (3) Carried, still open: `Follow.mutedAt`/`blockedAt` (`prisma/schema.prisma`) remain schema-only — `src/domains/follow/register.ts` only ever reads `blockedAt` (lines 68, 241, 264, 309 gate `FOLLOW_BLOCKED` / filter results), no function or route in `src/routes/follows.ts` sets or clears either field. Five logs running now. (4) Carried, still open: `src/services/playstore.ts` still hardcodes `installs30d`/`activeDevices30d` to `null` pending a BigQuery export from Play Console. (5) Carried, still open: version-surface drift — all routes still mounted under `/1-1-6/*` (`src/index.ts`) while `/app-config`'s `minVersion` fallback is hardcoded to `"1.1.9"` (`src/index.ts:123`), with no `/1-1-9` mount ever added. (6) Carried, still open: insight actionability — `src/routes/insights.ts` still exposes only `GET /insights` and `POST /insights/refresh` (whole-set regenerate), no per-suggestion accept/dismiss endpoint for `UserInsight` rows.
## ISSUE:ROADMAP 2026-07-27 07:06 ▸ Premium billing, follow block/mute, and Play Store metrics remain unshipped; two parallel review models now coexist; insight refresh still lacks per-item action

Reviewed against the 2026-07-13 log — the core carried gaps are unchanged in kind, and one new structural gap appeared alongside this quarter's moderation build-out. (1) Premium monetization: still no billing/subscription/IAP/receipt route anywhere in the codebase; `src/routes/admin.ts` `GET /admin/users` remains read-only with no role-change endpoint — premium is still only settable by direct DB edit. Three logs running with the same gap. (2) Follow block/mute: `Follow.mutedAt`/`blockedAt` (`prisma/schema.prisma`) remain schema-only — no function in `src/domains/follow/register.ts` or route in `src/routes/follows.ts` ever sets or clears either field; `blockedAt` is only ever read (gates `FOLLOW_BLOCKED` in `sendFollowRequest`, filtered out in `getFollowersPending`/`acceptFollowRequest`/`getProfile`'s `followStatus`). Four logs running now. (3) Carried, still open: `src/services/playstore.ts` hardcodes `installs30d`/`activeDevices30d` to `null` pending a BigQuery export from Play Console. (4) Version-surface drift persists: all routes still mounted under `/1-1-6/*` (`src/index.ts`) while `/app-config` `minVersion` default is `1.1.9` — three logs running. (5) New structural gap: two parallel review models now coexist — the legacy `RecipeReview` (plain stars, no status/moderation) has zero route surface anywhere in `src/routes/*.ts` (superseded by the new `Review` model with categories/moderation/accept-reject, `src/domains/review/register.ts`), yet no migration has dropped it — looks like orphaned schema left behind by the Review buildout. (6) Insight actionability improved but incomplete: `POST /insights/refresh` (`src/routes/insights.ts`, new since last log) lets a user manually re-trigger analysis, but there is still no per-insight accept/dismiss endpoint — `UserInsight` rows remain read-only content once generated, and refresh only regenerates the whole set rather than acting on a specific suggestion.
## ISSUE:ROADMAP 2026-07-13 07:04 ▸ Premium still has no grant path despite new timestamp model; follow block/mute states are unreachable; Play Store installs stub and display-only insights carried

Updated from 2026-07-06 entry — premium modelling advanced but the core gap remains, and the new follow system introduces a fresh one. (1) Premium was refactored this week to timestamp state (migration `20260711100000_premium_timestamps` added `premiumSince`/`premiumUntil`; `src/lib/premium.ts` `isPremium()` is the declared single source of truth; `src/middleware/rateLimit.ts` derives tier from it at request time), yet still no code path can set `premiumUntil`: there is no billing/subscription/IAP/receipt route anywhere, and `src/routes/admin.ts` exposes only a read-only `GET /admin/users` — premium remains grantable only by direct DB edit. Monetization is now better modelled but still unshipped. (2) New gap: the follow system (migration `20260711000000_add_follow_model`, `src/routes/follows.ts`) models `mutedAt`/`blockedAt` in `prisma/schema.prisma`, and `POST /follows/:userId` returns 403 `FOLLOW_BLOCKED` when `blockedAt` is set, but no endpoint ever sets or clears `mutedAt` or `blockedAt` — block and mute are unreachable states, so the moderation controls the schema promises are schema-only. (3) Carried, still open: `src/services/playstore.ts` permanently returns `installs30d`/`activeDevices30d` as `null` ("require BigQuery export from Play Console — not available via API alone"). (4) Carried, still open: insights remain display-only — `src/routes/insights.ts` exposes only `GET /insights`, no action endpoint for weekly `UserInsight` rows. (5) Version-surface drift: routes are still mounted under `/1-1-6/*` while `/app-config` `minVersion` default rose to `1.1.9` (`src/index.ts`) with no `/1-1-9` mounts, so the URL prefix no longer tracks the forced-upgrade version; legacy unversioned mounts remain live, and the legacy surface lacks a `/records` mount (cook records exist only at `/1-1-6/api/records`).
## ISSUE:ROADMAP 2026-07-06 07:13 ▸ Premium tier has no upgrade path; Play Store install metrics still stubbed; insight accept/apply flow removed leaving generate-only insights

Three gaps between API surface and implementation as of 2026Q3 start: (1) The `premium` role is fully modelled — `UserRole` enum in `prisma/schema.prisma`, elevated daily limits in `src/middleware/rateLimit.ts` (premium: ollama 10 / claude 5 vs free 3 / 2), and `authorIsPremium` surfaced in the discover feed (`src/routes/recipes.ts`) — but no code path can grant it: there is no billing/subscription/IAP/receipt route anywhere in the codebase, and `src/routes/admin.ts` exposes no role-change endpoint, so premium can only be set by direct DB edit. Monetization is implied by the schema but unshipped. (2) Carried from 2026Q2, still open: `src/services/playstore.ts` permanently returns `installs30d` and `activeDevices30d` as `null` (lines 59–61, "require BigQuery export from Play Console — not available via API alone") — store dashboards remain incomplete until a BigQuery pipeline exists. (3) The insight response flow was removed: migration `20260702000000_remove_insight_status` dropped the status column and `src/routes/insights.ts` now exposes only `GET /insights` — the 2026Q2 accept/auto-apply mechanism for dietary insights no longer exists, so weekly-generated `UserInsight` rows (`src/services/ai/insights.ts`) are display-only with no user action endpoint. Resolved since the 2026Q2 log: metrics CSV write path (now written by `appendMetric`/`appendDiscoverMetric` in `src/routes/recipes.ts`) and YouTube association (`findRecipeVideo` wired into recipe save and `GET /recipes/youtube`).
