MUST ASSET LOG
prompt: review and update ROADMAP ASSET compliance and business requirements for 2026Q3
path: must/ROADMAP-ASSET-2026Q3.md
target: {repo}

INSTRUCTION FOR AI MODEL:

YOU MAY READ AND UPDATE EXISTING ENTRIES AS REQUIREMENTS EVOLVE.
ADD NEW ENTRIES AT THE TOP FOR NEW TOPICS; UPDATE IN PLACE FOR EXISTING ONES.

FORMAT: ## ASSET:{NAME} {YYYY-MM-DD HH:MM} → {CONTENT}

####### <!-- ANCHOR MARKER - ADD OR UPDATE ENTRIES DIRECTLY BELOW THIS LINE -->
## ASSET:roadmap 2026-09-07 08:01 ▸ Self-join matching infrastructure shipped for both lists and users on one shared architecture; Preference gains the same rarity-weighted index Property already had

Delivered since the 2026-08-31 log. (1) `matchUsers` shipped (`src/domains/user/register.ts:537-575`, `GET /users/view/self/matches`): a rarity-weighted self-join on `Preference` (shared `(type, value)` pairs, scored by `1 / global frequency` via a `global_freq` CTE) returning a caller's top-10 matching users, deliberately built "structurally identical" (per its own comment) to the pre-existing `matchLists` self-join on `Property` in `src/domains/list/register.ts` — one matching pattern now serves both the list-discovery and user-discovery surfaces, sharing the same rarity-weighting logic and re-fetch-through-typed-Prisma convention (raw SQL only for the ID+score join, normal `findMany` for the response shape). (2) `Preference` gained `@@index([type, value])` (migration `20260828030000_preference_type_value_index`), the same index `Property` already carried for `matchLists` — added specifically to back `matchUsers`' new query, keeping the two matching paths symmetric at the schema level as well as the code level. (3) `Property` also gained its own dedicated `PropertyType` enum, decoupled from `PreferenceType` (migration `20260828000000_property_type_own_enum`), adding `ingredient`/`mealtime` as `Property`-only axes that must never become selectable `Preference` values (`mealtime` in particular is set at generate/save time, not user-chosen, per `prisma/schema.prisma`'s comment) — resolves a previously-shared-enum ambiguity now that `Property` and `Preference` are diverging in purpose. (4) Iteration discipline: a same-day `Personality` table add-then-revert (migrations `20260828010000`/`20260828020000`) shows a demographics-based matching approach was tried, backfilled, and cleanly reverted (data restored to `User.ageRange`/`gender` columns, no loss) once `Preference`-based matching proved sufficient — a real design iteration caught and undone within the same day rather than shipped and carried as debt. (5) Carried assets remain live: tier infrastructure centralized behind `getTier(user)` (`src/modules/rate/register.ts`) ready for a billing integration to plug into, admin visibility via `GET /admin/users`, App Store/Play Store connectivity, recipe provider/style enum hardening, moderation/report queue, categorized `Review` model, unified `Preference` model with 65-country granularity, bookmarks, public username/QR profile, metrics + digest pipeline, store observability suite. Open roadmap items carried — see paired ISSUE log, now including `matchUsers`' candidate pool being starved by the same unshipped premium-grant path.
## ASSET:roadmap 2026-08-31 08:31 ▸ Tier infrastructure already in place, ready for a billing integration to plug into

The groundwork for monetized tiers is solid and centralized, which narrows what a future billing integration needs to touch:
- Single source of truth: `UserRole` enum (`free`/`pro`/`super_pro`/`admin`) in `prisma/schema.prisma`, with `premiumSince`/`premiumUntil` already present on `User` for tracking active/lapsed status.
- Every tier-gated limit already resolves through one module — `src/modules/rate/register.ts`'s `getRatePreset`/`getListLimit`/`getInsightWindow`/`getDraftLimit`/`getEmailPinLimit`/`getListCourseLimit` — all keyed off `getTier(user)`, so granting a role automatically cascades into every existing limit with no per-feature changes needed.
- Admin visibility exists today: `GET /admin/users` (`src/routes/admin.ts`) already surfaces `role`, `premiumSince`, `premiumUntil`, and `roleLabel` per user, so an admin-side grant/revoke endpoint would slot next to an existing, already-authenticated (`requireAuth` + `requireAdmin`) route.
- App Store / Play Store connectivity already exists (`src/modules/appstore`, `src/modules/playstore`, using `APPSTORE_KEY_ID`/`APPSTORE_PRIVATE_KEY` and `PLAY_SERVICE_ACCOUNT_JSON` credentials) — the auth plumbing for hitting Apple/Google APIs is proven, even though it's currently scoped to analytics rather than purchases.
## ASSET:ROADMAP 2026-08-24 06:41 ▸ Recipe provider/style enums finalized against verified live data; four moderation columns de-enumed to TEXT; Agent table folded into User; UserInsight reshaped to Note-parity lifecycle

Delivered since the 2026-08-10 log. (1) `Recipe.provider` gained a real `RecipeProvider` enum (`ollama`/`claude`) via migration `20260818010000_add_recipe_provider_enum`, verified against live data before migrating (323 claude, 107 ollama, 2 null, no stray values, per `prisma/schema.prisma`'s comment) — closes the loop on which generation path actually produced each stored recipe (see paired ISSUE log for why this data also confirms `OpenAIProvider` is unreachable dead code). `Recipe.style` followed the same enum-hardening path, renamed `RecipeStyle`→`PreferenceStyle` across migrations `20260817000000_add_recipe_style_enum` → `20260820000000_recipe_style_rename` → `20260820010000_recipestyle_to_preferencestyle`, reframing style as a `Preference`-type concept first (`Preference{type:'style'}`) that `Recipe.style` merely snapshots at generation time. `Recipe` also gained a plain-text `method` column (migration `20260810010000_add_recipe_method`) recording the cooking-method used for that generation. (2) Four moderation/report enums went the opposite direction — `ListColor`, `FlagType`, `ReportReason`, `ReviewCategory` converted from Postgres enums to plain TEXT (migration `20260812160000_enum_to_string_categories`), validated app-side instead (`LISTCOLOR_CATEGORY`, `report/constants.ts`'s `FLAG_VALUES`/`REASONS`, `modules/review`'s `REVIEW_CATEGORY`) — a deliberate split from (1): stable+load-bearing values stay DB enums, volatile/cosmetic ones move to app-validated TEXT. (3) `Agent` table retired (migration `20260810000000_retire_agent_table`): its two columns (`language`, `languageUpdatedAt`) moved directly onto `User`, data copied before `DROP TABLE`. The standalone `GET /agents/me` endpoint noted in the 2026-08-03 log is gone; the same trust signal is now folded directly into `getMe`/`getProfile` responses (`src/domains/user/register.ts`, both self and friend view) via `user.language`/`languageLabel`/`languageApproved`/etc — a consolidation, not a feature loss. (4) `UserInsight` reshaped to match Note/Draft's lifecycle convention (migration `20260817010000_userinsight_slot_shape`): `createdAt`→`requestedAt` plus `status`/`acceptedAt`/`rejectedAt`/`removedAt`, `category` promoted from TEXT to a real `InsightCategory` enum, and the `(userId, category)` index tightened from non-unique to a real unique constraint (one slot per category, upserted in place). `InsightCategory` gained a 6th value, `method` (migration `20260818000000_insightcategory_add_method`), matching the cooking-method preference axis added in the prior log. Fields also renamed for Note-shape parity: `suggestion`→`body`, `context`→`title` (migration `20260819000000_userinsight_title_body`). One near-miss surfaced in the migration history: `20260817030000_add_userinsight_context`'s own comment notes `schema.prisma` had already declared `UserInsight.context` from a pulled commit before the matching migration existed — caught via `psql` before a production restart, not after a crash. (5) Carried assets remain live: self-service email change, three-tier rate limiting (free/pro/super_pro), unified `Preference` model with 65-country granularity, consolidated categorized `Review` model, report/flag moderation queue, bookmarks, public username/QR profile, metrics + digest pipeline, store observability suite. Open roadmap items carried — see paired ISSUE log.
## ASSET:ROADMAP 2026-08-10 06:38 ▸ Preferences unified into one Preference table; continent taxonomy replaced by 65-country granularity plus new cooking-method axis; standalone Note/lifecycle model shipped

Delivered since the 2026-08-03 log. (1) Preference storage unified: migration `20260805000000_unify_user_preferences` folds `User.recipeStyle`/`continentPreferences`/`defaultServings` and the old `DietaryPreference` table into a single `Preference` table (`type: dietary|continent|style|servings`, `value: string`), read/written through one door — `getUserPreferences`/`replacePreferences` in `src/modules/preference/register.ts` — replacing three previously-divergent storage shapes behind the same `PATCH /users/me/preferences` endpoint. (2) Continent taxonomy retired for country-level granularity: migration `20260808000000_continent_to_country` renames `PreferenceType.continent`→`country` and `Recipe.continent`→`country`, backed by a new 65-demonym `COUNTRIES`/`PREFERENCE_COUNTRY_CATEGORY` table (`src/modules/preference/constants.ts`) with flag emoji folded into `label`; verified zero real `continent` preference rows existed pre-migration, so only the 318 recipe-level tags (cosmetic SEO/profile-stat loss) were cleared. (3) New preference axis: cooking method (Bake/Grill/Fry/Steam/Boil/Roast/Raw/Slow Cook/Stir-fry/Air Fry) added via migration `20260809000000_add_preference_method`, wired into the Claude-provider prompt via `pickMethod`/`methodLine` in `src/modules/generate/register.ts` (see paired ISSUE log for the OpenAI-provider gap this introduces). (4) Standalone `Note` model shipped, replacing the short-lived `RecipeNote`/three-flat-column approach from the prior log: migrations `20260803010000_note_list_lifecycle`, `20260803020000_add_standalone_note`, and `20260804010000_retire_recipenote_for_note` land a single lifecycle-shaped `Note` table (freeform via `POST/PATCH/DELETE /notes`, `src/domains/note/register.ts`/`src/routes/notes.ts`) that also carries recipe-scoped description/ingredient/method notes via `recipeId`/`type`, both paths sharing the same AI language-moderation resolver (`runNoteModeration`). `SavedList`/`SavedListItem` gained the matching lifecycle shape (`status`/`requestedAt`/`acceptedAt`/`rejectedAt`) with combined title+description moderation (`runListCombinedModeration`, `src/domains/list/register.ts`). (5) `EmailPin` gained explicit `type`/`status`/`requestedAt`/`acceptedAt`/`rejectedAt` (migration `20260806010000_emailpin_lifecycle`), replacing implicit purpose-inference from `pendingEmail` presence with an explicit `signup|signin|email_update|delete` enum, same boundary-timestamp convention as Follow/Review/Note. (6) Carried assets remain live: self-service email change, three-tier rate limiting, consolidated star-free `Review` model, report/flag moderation queue, AI language moderation, bookmarks, public username/QR profile, `Agent` trust signal, metrics + digest pipeline, store observability suite. Open roadmap items carried — see paired ISSUE log.
## ASSET:ROADMAP 2026-08-03 07:05 ▸ Self-service email change shipped; super_pro tier added; Review model fully consolidated off stars

Delivered since the 2026-07-27 log. (1) Self-service email change flow shipped: `EmailPin.pendingEmail` (migration `20260728050000_email_pin_pending_email`) plus `POST /users/me/email` (issues a PIN to the new address via `issuePin(EMAILPIN_USER_EMAIL_UPDATE, ...)`) and `POST /users/me/email/confirm` (verifies the PIN, applies the new email + `emailVerifiedAt`) in `src/domains/user/register.ts` — email changes now require proving ownership of the new address before the account email moves, same trust model as signup. (`PATCH /users/me` still hard-rejects any `email` field with `EMAIL_CHANGE_REQUIRES_VERIFICATION`, directing callers to the new flow.) (2) Third paid tier added: `super_pro` joins `free`/`pro`/`admin` in `UserRole` (migration `20260728000000_user_role_super_pro`), with its own rate preset (`RATE_SUPER_PRO`, `src/modules/rate/constants.ts`) — expands the pricing surface (see paired ISSUE log for the still-missing grant path). (3) `Review` model fully consolidated off the legacy `stars` field: migration `20260802000000_review_drop_stars` removes it, leaving `ReviewCategoryRating` (taste/difficulty/authentic) as the sole rating mechanism on the moderated review path, while the separate lightweight `RecipeReview.stars` (discover-feed ranking signal) continues unaffected. (4) "Last opened" read markers renamed/generalized: migrations `20260802000000_add_opened_at` through `20260802020000_review_follow_sender_receiver_opened_at` replaced ad hoc sender/receiver-opened columns with symmetric `selfOpenedAt`/`friendOpenedAt` on both `Follow` and `Review`. (5) Carried assets remain live: report/flag moderation queue, categorized review system, AI language moderation on reviews and follow comments, bookmarks, public username/QR profile, `Agent` trust signal, metrics + digest pipeline, store observability suite. Open roadmap items carried: premium billing/grant path (now three tiers deep), follow block/mute endpoints, Play Store BigQuery pipeline, per-insight accept/dismiss action, `/1-1-6` vs `1.1.9` version-surface drift.
## ASSET:ROADMAP 2026-07-27 07:06 ▸ Content moderation/trust surface shipped: recipe & user flagging, structured multi-category reviews, AI language moderation, likes, and public usernames

Major delivery since the 2026-07-13 log, all net-new trust/social infrastructure on top of the carried follow/visibility/premium/metrics base. (1) Report/flag system shipped: `RecipeReport`/`UserReport` models (migration `20260715100000_recipe_user_reports`) support red/green flags with reason codes (`spam`/`offensive`/`wrong_info`/`other`), re-flagging reuses the row and resets to pending, and an admin moderation queue (`GET/PATCH /admin/reports/{recipes,users}`) runs a pending→accepted/rejected→closed lifecycle with `reviewedAt`/`reviewedBy` audit fields (`src/domains/report/register.ts`, `src/routes/admin.ts`, `src/routes/reports.ts`). (2) Structured review system shipped: the new `Review` model requires 3 category ratings (taste/difficulty/authentic via `ReviewCategoryRating`, one row per category enforced at submit), gives the recipe author an accept/reject/response workflow, and every submitted/re-submitted review is auto-checked by an AI language-moderation pipeline (`checkLanguage`/`LANGUAGE_REVIEW`) that writes both the review's own durable `languageVerdict` and the reviewer's rolling `Agent.language` signal (`src/domains/review/register.ts`, `src/domains/agent/register.ts`). (3) The same language-moderation pipeline was extended to social requests: the optional `comment` on `POST /follows/:userId` is checked via `LANGUAGE_FOLLOW` and surfaced to clients as `languageMessage`, without ever gating the follow decision itself. (4) Bookmark ("likes") system shipped: `Bookmark` pair table with re-like/un-like via row reuse, `POST/DELETE /bookmarks/:recipeId`, and `GET /bookmarks` returning full recipe cards (`src/domains/bookmark/register.ts`). (5) Public identity/discovery shipped: unique `username` column (letter+digit app-level rule), `GET /users/me/qr` QR code endpoint, and `getProfile` now resolves by id-or-username so renames don't break existing profile links. (6) `Agent` model + `GET /agents/me` expose a per-user rolling trust signal — most-recent language verdict across any checked content (reviews, follow comments), designed to grow more tracked dimensions later. (7) Carried assets remain live: follow request/accept/visibility system, premium timestamp model, metrics CSV pipeline + daily digest, YouTube association, versioned `/1-1-6/*` surface, store observability suite. Open roadmap items carried plus newly noted: premium billing/grant path, follow block/mute endpoints, Play Store BigQuery pipeline, per-insight accept/dismiss action, and the orphaned `RecipeReview` model pending cleanup.
## ASSET:ROADMAP 2026-07-13 07:04 ▸ Social follow graph with visibility controls shipped; premium converted to auditable timestamp model; account-security state made explicit

Updated from 2026-07-06 entry — major social/identity delivery this week on top of the carried Q2 closures. (1) Follow system shipped: migration `20260711000000_add_follow_model` plus `src/routes/follows.ts` implement a timestamp-only state machine (pending → accepted via `PATCH /follows/:id/accept`; unfollow/reject end the edge; re-follow reuses the row and returns to pending requiring re-approval), with follower/following/pending lists and friend-view endpoints (`GET /follows/:userId/following|followers`) gated by the new `followVisibility` setting. (2) Visibility overhaul: migrations `20260712110000_visibility_enum_replaces_share_tokens` and `20260712120000_visibility_default_public` replaced share tokens with a `Visibility` enum; per-list `PATCH /lists/:id/visibility` and `PATCH /follows/visibility` give users granular friend-view control (default public). (3) Premium re-modelled as auditable timestamp state (`premiumSince`/`premiumUntil`, `src/lib/premium.ts`), lapse-aware by construction; `src/middleware/rateLimit.ts` derives free/premium/admin tier at request time with an atomic Redis Lua increment — billing groundwork in place. (4) Account-security state now explicit: `emailVerifiedAt` (`20260711110000`) and `passwordSetAt` (`20260711130000`) replace booleans, and `GET /admin/users` (`src/routes/admin.ts`) reports linked providers, premium, and verification state per user. (5) Carried assets remain live: metrics CSV pipeline + daily digest (`src/digest.ts`), YouTube association, versioned `/1-1-6/*` surface with legacy mounts, forced-upgrade `minVersion` (env default now 1.1.9), and store observability (crash/ANR via `src/services/appstore.ts`/`playstore.ts`). Open roadmap items: premium grant/billing path, follow block/mute endpoints, Play Store BigQuery pipeline, insight action endpoints.
## ASSET:ROADMAP 2026-07-06 07:13 ▸ Metrics pipeline and YouTube association shipped closing two Q2 gaps; versioned /1-1-6 API live; flow system and insight-status deliberately retired

Delivered entering 2026Q3: (1) End-to-end metrics pipeline — `src/routes/recipes.ts` writes `would/RECIPE-METRIC.csv` (19-column row incl. provider, fallback, responseMs, pantry/grocery match pcts, promptVersion) and `would/DISCOVER-METRIC.csv`; `src/digest.ts` reads both and posts a daily Google Chat digest with per-model stats and an Ollama-generated summary; `!metrics` in `src/routes/chat.ts` now returns real counts — closing a 2026Q2 ISSUE. (2) YouTube video association shipped: `src/services/youtube.ts` is routed via `findRecipeVideo` on recipe save (persisting `Recipe.videoId`) plus standalone `GET /recipes/youtube` — closing the second Q2 gap. (3) Versioned API surface live: all routes mounted under `/1-1-6/*` with legacy unversioned mounts kept until old builds phase out, plus `/app-config` `minVersion` (env `MIN_APP_VERSION`, default 1.1.6) enabling forced-upgrade control. (4) Deliberate scope retirements this quarter: flow system removed (migration `20260703000000_remove_flows_model`; `src/routes/flows.ts` deleted) and insight status dropped (`20260702000000_remove_insight_status`), simplifying insights to latest-per-category across dietary/cuisine/style/pantry/mealType. (5) Store observability suite in place: `src/services/appstore.ts` + `src/services/playstore.ts` (crash/ANR rates) behind `src/routes/storeMetrics.ts` and scheduled `src/storeReport.ts`. Open roadmap items: premium upgrade/billing path, Play Store BigQuery pipeline, insight action endpoints.
